In this Rust repo (crates/tunnel2tunnel-ssh), find where direct-tcpip channel-open requests get rejected with "administratively prohibited" (this is russh's default rejection reason string, ChannelOpenFailure::AdministrativelyProhibited). Look in crates/tunnel2tunnel-ssh/src/*.rs for channel_open_direct_tcpip handler.

Context: log line "channel_open_direct_tcpip" resolves hostname → checks access → splices channels. Two client entities exist: "Fedora Work" (client) requesting host="m1n" port=5900. Earlier in the session, host="PC6" port=5900 requests failed with "target server never came online within the retry window" (a distinct, logged rejection path). Then a request for host="m1n" succeeded and logged "direct-tcpip: bridge established ... client_ch=5 server_ch=2" (this log line appears TWICE with identical nanosecond timestamp in the t2t log — note whether that's a real double-bridge bug or just a duplicated log call).

I need to know: what are ALL the distinct code paths in channel_open_direct_tcpip (or whatever function handles this) that return a rejection vs Ok, and specifically which one is silent (returns Err/rejection without a corresponding info!/warn! log line) — since the client-side ssh log shows 'channel 5: open failed: administratively prohibited: Rejected' but there's no matching t2t-side log line for a channel 5 rejection (only two 'target server never came online' rejections for PC6, and one successful bridge-established for m1n, logged twice).

Report: file:line for the handler function, every return/reject branch with whether it logs, and specifically whether the bridge-established path could itself later fail/close in a way that would produce a second automatically-prohibited-style rejection on a *reused* or *new* channel without logging. Also check if there's channel reuse/multiplexing logic where a single SSH connection's "channel 5" might get opened twice (e.g., VNC client retry) and note anything suspicious about the duplicate log line at server_ch=2/client_ch=5 appearing twice.