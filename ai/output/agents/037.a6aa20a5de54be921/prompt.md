Researching a fix for a bug in a Rust SSH server built with the `russh` crate, version 0.61 (check Cargo.lock / crates/tunnel2tunnel-ssh/Cargo.toml for exact version pin).

Bug: `channel_open_direct_tcpip` in crates/tunnel2tunnel-ssh/src/lib.rs (~line 1064-1293) runs a blocking retry-wait loop (up to 12s, `tokio::time::sleep` in a loop, ~line 1214-1233) directly inside the async `Handler` trait method, before returning `Ok(bool)`. Per this repo's CLAUDE.md, russh `Handler` callbacks execute on the connection's single-task event loop, so awaiting/sleeping inline here blocks ALL other channel activity on that same SSH connection (other channel-open requests, and data forwarding on already-established channels) for the duration of the sleep.

I need to know russh 0.61's actual mechanism for deferring a channel-open-direct-tcpip confirmation so the handler can return immediately and confirm/reject the channel later from a spawned task, WITHOUT blocking the event loop. Specifically:

1. Does `channel_open_direct_tcpip` in russh 0.61's `Handler` trait return `Result<bool, Error>` where the bool is sent as an immediate confirm/reject to the client by the calling code, or is there a way to return "pending" and confirm later via a `Session`/`Handle` method (e.g. something like `session.channel_open_confirmation(...)`, `handle.channel_success/failure(...)`, or similar)?
2. Look at how this same codebase already solved an analogous problem — CLAUDE.md says `channel_open_session()` confirmation calls must be `tokio::spawn`ed, never awaited inline from `auth_succeeded`. Find that code (grep for `channel_open_session` and `tokio::spawn` near auth handling in lib.rs) and describe the exact pattern used, since we want to mirror it here.
3. Check the actual `russh` crate source (in ~/.cargo/registry or wherever the dependency is vendored/downloaded — try `find / -path '*/russh-0.6*/src/server/mod.rs' 2>/dev/null` or search cargo registry cache) for the `Handler` trait's `channel_open_direct_tcpip` signature and how the server loop dispatches on its return value — specifically what happens on the `Session` side when a channel-open request is received: does the session event loop wait synchronously for the Handler future to resolve before processing the next incoming message on that connection, or is each incoming request's handler future spawned/polled independently allowing concurrent channel-opens to interleave?

Report back precisely: whether the fix should be (a) spawn a tokio task that does the retry-wait then calls some Session/Handle API to confirm/reject the channel asynchronously while the handler itself returns quickly, and what that exact API call looks like with correct types/imports, or (b) something else entirely (e.g. the real fix is that russh already processes each channel request on its own task, and the actual observed blocking is caused by something else). Include file:line citations from the vendored russh source and from this repo's existing code.