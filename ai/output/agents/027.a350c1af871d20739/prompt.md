Repo: /home/user/git/luckydonald/tunnel2tunnel (Rust workspace: tunnel2tunnel-core, tunnel2tunnel-web, tunnel2tunnel-ssh, t2t binary; Postgres via SQLx). Read CLAUDE.md at repo root first — it documents conventions (Axum 0.8 `{param}` syntax, russh 0.61 quirks, SQLx `Timestamps` vs `TimestampsSoftDelete` struct nesting, etc.) that you must follow.

An approved implementation plan is written at `/home/user/.confuig/claude/accounts/private/plans/robust-bubbling-ember.md` — **read this file in full first**, it is the source of truth for what to build. This task is the **backend portion only** (plan's "Sequencing" steps 1-3: schema+core models, SSH-layer enforcement, web routes). Do NOT touch the frontend (`frontend/` directory) — that's a separate follow-up task.

## Summary of what you're building (full detail is in the plan file)

Replace the current `entity_ports` + `entity_port_discovery_rules` two-sided config model with:
1. `port_configs` (renamed from `entity_ports`, drops `server_entity_id`, `name` becomes `NOT NULL`) — the single declaration "entity X offers this port as a service."
2. `port_subscriptions` (new table, replaces `entity_port_discovery_rules` entirely) — M2M: `(port_config_id, subscriber_entity_id, subscriber_local_port, enabled)`.
3. `entity_access` gains an optional `port_config_id` column (null = whole-entity grant as today, set = scoped to one port).
4. `entities.type` column is dropped entirely; "server"/"client" become computed booleans (`is_server` = owns ≥1 `port_configs` row, `is_client` = owns ≥1 `port_subscriptions` row) in the entity response.

Two new migrations, next available numbers are `014` and `015` (current highest is `013_tarpit_ban_soft_delete.sql` in `migrations/`):
- `014_port_configs_subscriptions.sql`
- `015_drop_entity_type.sql`

## Current code you'll be modifying (verified by direct reads during planning — use these as your starting points)

**Migrations**: `migrations/002_entities_keys_ports.sql` (entities/entity_ports tables), `migrations/003_access_friends.sql` (entity_access/friendships), `migrations/005_port_discovery.sql` (entity_port_discovery_rules — to be dropped), `migrations/006_entity_port_server_ref.sql` (adds server_entity_id — to be dropped), `migrations/007_entity_port_host.sql` (adds host column — keep).

**`crates/tunnel2tunnel-core/src/models/entity_port.rs`**: `EntityPort` struct (id, entity_id, enabled, local_port, proxy_port, name: Option<String>, description, sort_order, host, server_entity_id: Option<Uuid>, ts: Timestamps). Methods: `list_for_entity`, `create`, `update`, `find_by_id`, `delete`. Rename this to represent `port_configs` (rename struct to `PortConfig`, drop `server_entity_id` field, change `name` to plain `String` (no longer optional), add `find_enabled_by_entity_and_proxy_port(pool, entity_id, proxy_port) -> Result<Option<Self>, CoreError>`. You may rename the file to `port_config.rs` (update `crates/tunnel2tunnel-core/src/models/mod.rs` accordingly) or keep the filename — your call, but be consistent and update every import site.

**`crates/tunnel2tunnel-core/src/models/entity_port_discovery_rule.rs`**: DELETE this file (remove from `mod.rs`). It has `EntityPortDiscoveryRule` (CRUD: find/upsert/delete) and `list_reachable_for_client(pool, client_entity_id, client_user_id) -> Vec<ReachableServer>` (joins entity_access + entity_ports). Replace with a new `crates/tunnel2tunnel-core/src/models/port_subscription.rs`: `PortSubscription` struct (id, port_config_id, subscriber_entity_id, subscriber_local_port, enabled, ts: Timestamps). CRUD: `create`, `update` (subscriber_local_port and/or enabled), `delete`, `find_by_subscriber_and_port_config(pool, subscriber_entity_id, port_config_id)`. Plus the browse query `list_subscribable_for_entity(pool, subscriber_entity_id, subscriber_user_id) -> Result<Vec<SubscribableOwner>, CoreError>` where:
```rust
pub struct SubscribableService { pub port_config: PortConfig, pub subscription: Option<PortSubscription> }
pub struct SubscribableOwner { pub entity: Entity, pub services: Vec<SubscribableService> }
```
This query should union: (a) entities where the querying entity/user has a whole-entity `entity_access` grant (`port_config_id IS NULL` in the grant row) — include ALL of that owner's enabled port_configs; (b) port_configs that have a port-scoped `entity_access` grant (`port_config_id` set) naming this subscriber/user, even if there's no whole-entity grant for that owner. For each returned port_config, LEFT JOIN the subscriber's own `port_subscriptions` row if one exists (so the frontend can show subscribed-vs-not). Model this after the existing `list_reachable_for_client` query in the file you're deleting — same join shape, just extended for port-scoped grants and restructured around port_configs/port_subscriptions instead of the old client-owned EntityPort rows. Also implement the same-account bypass here: if `subscriber_user_id` owns an owner entity outright (i.e. the port_config's owner entity has the same `user_id`), it's always includable regardless of any `entity_access` row — mirror this per the plan's "same-account access is implicit" rule.

**`crates/tunnel2tunnel-core/src/models/entity_access.rs`**: `EntityAccess` struct — add `port_config_id: Option<Uuid>` field. `check_access(pool, target_entity_id, client_entity_id, client_user_id, target_user_id) -> Result<bool, CoreError>` (lines ~87-114) — extend to `check_access(pool, target_entity_id, port_config_id: Option<Uuid>, client_entity_id, client_user_id, target_user_id)`, adding `AND (port_config_id IS NULL OR port_config_id = $new_param)` to the existing `EXISTS` predicate (a NULL-scoped grant row still covers a specific port_config_id request; a grant scoped to a specific port_config_id only matches that same id). **Also add a same-account short-circuit**: if `client_user_id == target_user_id`, return `Ok(true)` immediately without querying the DB at all (this is the "you always have access to your own services" rule) — do this in every caller of `check_access`, or centralize it inside `check_access` itself (simpler: put it at the top of the function).

**`crates/tunnel2tunnel-core/src/models/entity.rs`**: `Entity` struct has an `entity_type: String` field sourced from the `type` column. Remove this field entirely (and any validation of `'server'|'client'` values). Check `crates/tunnel2tunnel-web/src/routes/settings.rs` too — it was flagged as referencing entity_type/entity_access, check what for and adjust.

**`crates/tunnel2tunnel-ssh/src/lib.rs`** — the SSH routing crate:
- `tcpip_forward` (~line 909-939): registers `(entity_id, port)` into `server_slots: Arc<Mutex<HashMap<(Uuid,u32),(Handle,String)>>>` unconditionally today. Add: before/alongside that insert, look up `PortConfig::find_enabled_by_entity_and_proxy_port(pool, entity_id, port)`; if `None`, auto-create one via `PortConfig::create` with `local_port = port`, `proxy_port = port`, `host = "localhost".to_string()`, `enabled = true`, `name = guess_service_name(port as i32).unwrap_or("Unnamed Service").to_string()`, `description = None`, `sort_order` = 0 (or query current max+1 for that entity — check `list_for_entity`/existing `create` signature for how sort_order is currently handled on manual creation and mirror it, simplicity is fine here). Create the new `crates/tunnel2tunnel-core/src/port_names.rs`: `pub fn guess_service_name(port: i32) -> Option<&'static str>` — a `match` covering at least: 21 FTP, 22 SSH, 23 Telnet, 25 SMTP, 53 DNS, 80 HTTP, 110 POP3, 143 IMAP, 443 HTTPS, 445 SMB, 465 SMTPS, 587 "SMTP (submission)", 993 IMAPS, 995 POP3S, 1433 MSSQL, 1521 "Oracle DB", 2049 NFS, 3000 "Node.js (dev)", 3001 Grafana, 3306 "MySQL / MariaDB", 3389 RDP, 5000 "Flask / dev", 5432 PostgreSQL, 5900 VNC, 6379 Redis, 6443 "Kubernetes API", 7777 "Game server", 8000 "HTTP (dev)", 8006 Proxmox, 8080 "HTTP (alt)", 8081 "HTTP (alt 2)", 8096 Jellyfin, 8123 "Home Assistant", 8384 Syncthing, 8443 "HTTPS (alt)", 8880 phpMyAdmin, 8888 Jupyter, 9000 "Portainer / MinIO", 9090 Prometheus, 9091 Transmission, 9100 "Node exporter", 9200 Elasticsearch, 11211 Memcached, 25565 Minecraft, 27015 "Steam / Source", 27017 MongoDB, 32400 Plex. Add `pub mod port_names;` to `crates/tunnel2tunnel-core/src/lib.rs`.
- `channel_open_direct_tcpip` (~line 972-1097): currently resolves target entity (via UUID parse or `EntityAccess::find_entity_by_hostname` alias), calls `EntityAccess::check_access` (whole-entity only), then looks up `server_slots` by `(target_entity_id, port_to_connect)`, rejecting immediately if any step fails. Replace the authorization portion with this exact 4-step sequence (see plan file's "Backend enforcement (SSH layer)" section for full rationale — implement precisely as described there):
  1. `PortConfig::find_enabled_by_entity_and_proxy_port(pool, target_entity_id, port_to_connect as i32)` — reject (`return Ok(false)`, distinct log line) if `None`.
  2. `PortSubscription::find_by_subscriber_and_port_config(pool, client_entity.id, port_config.id)` — reject if `None` or found-but-`enabled == false`.
  3. If `client_user_id != target_entity.user_id`: call the extended `EntityAccess::check_access` (with `Some(port_config.id)`) — reject if `false`. (If same user, this step is skipped — `check_access`'s internal short-circuit handles it too, but skip the call entirely here for clarity/efficiency, your call.)
  4. Look up `server_slots.get(&(target_entity_id, port_to_connect))`. If missing, retry: poll every 250ms for up to ~12 seconds (use `tokio::time::sleep` in a loop, or `tokio::time::timeout` wrapping a polling loop) before finally rejecting if it never appears. If it appears mid-wait, proceed with the existing bridging logic unchanged.
  Add distinct `tracing::warn!`/`tracing::info!` log lines per rejection reason (port not configured / not subscribed / access denied / server never came online) so operators can distinguish these in logs — this matters operationally, don't collapse them into one generic message.

## Web routes — `crates/tunnel2tunnel-web/src/routes/entities.rs` and `access.rs`

- Rename/adjust the ports CRUD routes (`list_ports`, `create_port`, `update_port`, `delete_port`, ~lines 353-424) to operate on `PortConfig`/`port_configs`; `CreatePortBody`/`UpdatePortBody` (~lines 52-73) drop `server_entity_id`, keep the rest, `name` becomes required (non-Option) in `CreatePortBody` and validated non-empty.
- Delete `list_reachable_servers`/`set_port_discovery_state` (~lines 450-561) and their request/response types (`DiscoveredPortResponse`, `ReachableServerResponse`, `SetDiscoveryStateBody`). Replace with:
  - `GET /api/entities/{id}/subscribable-services` → wraps `PortSubscription::list_subscribable_for_entity`, response mirrors `SubscribableOwner`/`SubscribableService` (flatten into a JSON-friendly response struct).
  - `POST /api/entities/{id}/subscriptions` (body: `{port_config_id, subscriber_local_port}`) — validates `EntityAccess::check_access` (or the same-account bypass) server-side before inserting via `PortSubscription::create`.
  - `PUT /api/entities/{id}/subscriptions/{subscription_id}` (body: `{subscriber_local_port?, enabled?}`).
  - `DELETE /api/entities/{id}/subscriptions/{subscription_id}`.
  All gated by `require_owner` the same way existing port/access routes are (look at how `require_owner`/`require_entity_owner` are used elsewhere in this file and in `access.rs` — reuse that exact pattern).
- `EntityResponse` (~lines 77-125 in `entities.rs`, alongside the existing `online`/`last_disconnected_at` derivation via `ConnectionLog::entity_status(es)`): add `is_server: bool` and `is_client: bool`, computed via a `COUNT(*) > 0` query against `port_configs`/`port_subscriptions` for that entity_id (add a small helper method on `PortConfig`/`PortSubscription`, e.g. `PortConfig::entity_has_any(pool, entity_id) -> Result<bool, CoreError>`, or batch it the same way `ConnectionLog::entity_statuses` batches for `list_entities`/`get_entity` — check how that batching works at ~line 223-241/266-282 and mirror it so listing many entities doesn't do N+1 queries). Drop `entity_type` from `CreateEntityBody`/`UpdateEntityBody`/`EntityResponse` entirely (no more type field anywhere in entity requests/responses). Add optional `role=server|client` query param support to whatever the list-entities query handler is, filtering by the same computed booleans.
- `access.rs`: `CreateAccessBody`/`create_access` (~lines 84-109) — add optional `port_config_id: Option<Uuid>`, validated (if present) to reference a `port_configs` row owned by the same `owner_entity_id` the rule is being created on (reject with `BadRequest` otherwise). Pass through to `EntityAccess::create`.

## Verification (do this before each commit, not just at the end)

- `cargo build` (and `cargo test` if the crate has existing tests — check `crates/t2t/tests/tunnel_e2e.rs`, which was flagged as referencing `entity_port`/`entity_type`/`entity_access`; update it to match the new model rather than leaving it broken/stale — read it first to see what it currently exercises).
- SQLx: this project likely uses either a live DB connection or `.sqlx` offline query cache for compile-time query checking — check `crates/*/Cargo.toml`/`.cargo/config.toml`/root `Cargo.toml` and any `.sqlx/` directory for `SQLX_OFFLINE`. If offline mode is used, you'll need `cargo sqlx prepare` (or equivalent workspace-wide `--workspace` variant) after schema changes, against a real running Postgres with the new migrations applied — CLAUDE.md documents how to start Postgres via podman (`docker.io/postgres:18-alpine`) and run migrations (they apply automatically on `cargo run -p t2t` startup, or check for a migration-runner binary/test). If no such cache exists and queries are checked live, just make sure a local Postgres with migrations applied is reachable while building.
- Do NOT touch anything under `frontend/`.

## Committing

The `commit-with-lplp-style` skill is active for this repo (see `ai/skills/commit-with-lplp-style/SKILL.md` at the repo root) — follow its commit message format exactly: `[where] topic: ai: Run: <summary>.` (e.g. `[backend] port configs: ai: Run: Replaced entity_ports/discovery rules with port_configs + port_subscriptions and added port-level SSH enforcement.`), write the message to `ai/git/pending-commit.md` first via the Write tool, then `git commit -F ai/git/pending-commit.md`, staging only the files you actually changed by explicit path (never `git add -A`/`git add .`). Make 2-3 commits total for natural checkpoints (e.g. one for migrations+core models, one for SSH enforcement, one for web routes) rather than one giant commit or one-per-file — use your judgement on natural breakpoints, each commit should build (`cargo build` passes) on its own if reasonably possible.

Report back: what you built, any deviations from the plan and why, the exact new/changed public API shapes (route paths, request/response JSON shapes) so a follow-up frontend task can consume them precisely, and the state of `cargo build`/`cargo test` at the end.