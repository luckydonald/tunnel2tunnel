Explore the tunnel2tunnel frontend codebase at /home/user/git/luckydonald/tunnel2tunnel/frontend/src. I need to understand:

1. The `SshCommandDisplay` component — what props it takes, how it generates the SSH command, what it currently shows
2. The `EntityDetail` page — how it uses SshCommandDisplay, what data it loads, how it distinguishes "client" vs "server" sections
3. The `entities.ts` API — what data is available about entity ports and access rules (entity_access)
4. The `entities` store — what state it holds

Focus on understanding: what ports data is currently shown to a client entity, and how the SSH command is currently generated. Search thoroughly.