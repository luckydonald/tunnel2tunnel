Investigate tunnel2tunnel repo bug: when client connects with a tunnel matching webui command, client shown online, but port still shown gray (should show connected/active state). Also: "remote is not connected" state should be shown as outer ring of status icon being orange, while main dot color stays tied to client connected/disconnected — these two are currently conflated and need to be independent.

Research needed (no code changes, just report facts with file paths and line numbers):

1. Find the frontend component(s) rendering entity/port status icons — likely under frontend/src/components or frontend/src/pages (e.g. Entities.vue, EntityDetail.vue, or a status-icon component). Find where "online"/"gray"/status colors are computed for ports vs client/entity.
2. Find how "remote is not connected" state is currently represented/displayed — search for "remote" "not connected" strings in frontend/src.
3. Find backend/API state that reports: client online status, per-port connection status, and "remote connected" status — likely in tunnel2tunnel-web routes (entities.rs or similar) and tunnel2tunnel-ssh (tunnel router, port slots HashMap). Look at crates/tunnel2tunnel-ssh/src for tcpip_forward / channel_open_direct_tcpip handling and how server slots map to ports, and how "server-liveness" (mentioned in recent commits) ties in — check recent commit 213f97a and 9580799 context, project memory mentions "Port discovery architecture" with three-state model, migration 005-006, two-query pattern.
3b. Read the memory file /home/user/.claude/projects/-home-user-git-luckydonald-tunnel2tunnel/memory/project_discovery_architecture.md if it exists, and summarize its three-state model and two-query pattern.
4. Find existing e2e test suite structure (search for tunnel_e2e or similar rust integration tests, and any frontend e2e tests) so we know where to add new API/state test scenarios covering: client connected+port active, client connected+port gray/inactive (the bug), client disconnected, remote-not-connected variants.
5. Identify root cause: why does port stay gray even though client is marked online — is it a stale query, a state derived from a different source not being refreshed, a websocket/polling gap, or a data model issue where "port active" requires a distinct signal that isn't being set?

Report back: relevant file paths + line numbers for the status computation logic (frontend and backend), the existing color logic, the existing three-state architecture, root cause hypothesis for the gray-port bug, and where e2e tests should be added. Keep report focused and around 500-800 words, with concrete file:line references.