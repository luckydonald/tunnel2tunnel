In /home/user/git/luckydonald/tunnel2tunnel (Rust backend, crates/tunnel2tunnel-web and tunnel2tunnel-ssh and tunnel2tunnel-core), I need to understand how the existing live-connections websocket push mechanism works, to design a NEW real server-side "event" push (not just full-state snapshot re-push) for toast notifications on the frontend.

Context: frontend already has `/api/me/live-connections/ws`, `/api/entities/{id}/live-connections/ws`, `/api/admin/live-connections/ws` — these currently push the FULL current state snapshot every time something changes (frontend just overwrites local state with whatever it receives, per `useLiveSocket.ts`). Git log shows recent commits "Made status badges and admin Live Connections realtime over WebSocket" and status is tracked via a `RemoteStatus` enum (`offline`/`not_forwarded`/`idle`/`active`) plus a `live: bool` flag.

I need to find:
1. Where/how the backend currently triggers a push to these websocket connections when state changes (e.g. a broadcast channel, a `tokio::sync::watch` or `broadcast::Sender`, or a Postgres LISTEN/NOTIFY, or a shared `Arc<Mutex<...>>` scanned on an interval). Find the exact file/function that decides "state changed, push now" — is it truly event-driven from the SSH tunnel accept/close code paths, or is there a polling loop somewhere?
2. Where the SSH side (crates/tunnel2tunnel-ssh) accepts a connection, opens/closes a tunnel, or changes forwarding status (tcpip_forward, channel_open_direct_tcpip, disconnect) — trace what notifies the web layer that state changed. File:line for each transition point (entity goes live, port starts forwarding, bridging starts/stops/idle, entity goes offline).
3. What data structure represents "current live status" server-side (probably in tunnel2tunnel-core) — its fields, and whether it's keyed in a way that would let us compute a diff (old value vs new value) SERVER-SIDE at the point of mutation, so we could emit a small discrete event (e.g. `{type: "port_forwarding_started", entity_id, service_name}`) instead of the client diffing snapshots.
4. Whether there's already any per-user (not per-entity, not admin-wide) aggregation point where all of a logged-in user's own entities' events could be pushed on one shared channel/websocket route regardless of which frontend page/route is active. Is `/api/me/live-connections/ws` scoped correctly for this (i.e. does it already restrict to entities owned by the session's user)?
5. Any existing session/auth extraction pattern in the websocket upgrade handlers (how does the handler know which user is connecting) — file:line.

Report file:line references and code snippets for the key pieces (the broadcast/notify mechanism, the SSH-side mutation points, the websocket handler for `/api/me/live-connections/ws`). This feeds an implementation plan — be thorough and precise, quote exact function signatures.