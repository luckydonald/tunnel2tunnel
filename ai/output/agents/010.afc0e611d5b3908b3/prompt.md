I'm working in the Rust workspace at /Users/user/Documents/programming/Rust/tunnel2tunnel (a self-hosted SSH rendezvous manager: tunnel2tunnel-core has DB models, tunnel2tunnel-web is an Axum HTTP API, tunnel2tunnel-ssh is a russh SSH server, t2t is the binary that joins all three).

I need to add a new automated integration test that replicates a manual test procedure: start the full t2t binary (HTTP+SSH) against a real Postgres 18 database, create two "entities" (server + client) via the HTTP API with generated SSH keypairs, grant access between them, then spawn two real `ssh` CLI subprocesses — one doing `ssh -R proxy_port:127.0.0.1:local_port` (registers a remote forward) and one doing `ssh -L local_port:target_entity_uuid:proxy_port` (consumes it) — and verify traffic flows end-to-end through the tunnel by curling the locally forwarded port.

Please research and report back (don't write any code, this is read-only research):

1. Does this workspace have ANY existing test code today? Search all crates (`crates/*/src/**`, `crates/*/tests/**`) for `#[test]`, `#[tokio::test]`, `#[sqlx::test]`, or a `tests/` directory. Report exactly what exists, if anything, and where.
2. Check `Cargo.toml` (workspace root) and each crate's `Cargo.toml` for `[dev-dependencies]` — is `tempfile` a dependency already (I saw it compile during a `cargo build`)? Is `reqwest` already a dependency anywhere (used for what)? Is `sqlx` configured with the `test-util` or `migrate` features? Are there testcontainers, or any existing patterns for spinning up a test database?
3. Look at `crates/tunnel2tunnel-web/src/lib.rs` and `crates/tunnel2tunnel-ssh/src/lib.rs` for the `start()`/`WebConfig`/`SshConfig` entry points, and `crates/t2t/src/main.rs` — specifically: can the HTTP server and SSH server be started programmatically bound to OS-assigned ephemeral ports (port 0), or do they need fixed ports? Can multiple instances run in the same test process (i.e., is there any global/static state that would prevent running the servers twice in one process, e.g. in different tests)?
4. Look at `crates/tunnel2tunnel-web/src/bootstrap.rs` for how the admin user gets created, and `crates/tunnel2tunnel-web/src/routes/auth.rs` for the login endpoint — I want to know if I can create entities/keys/access rules by calling the core model functions directly (`Entity::create`, `SshKey::create`, `EntityAccess::create` in `tunnel2tunnel-core/src/models/`) instead of going through the HTTP API with session cookies, to keep a test simpler.
5. Check `crates/tunnel2tunnel-ssh/src/lib.rs`'s `load_or_generate_host_key` and how it generates Ed25519 keys with `russh::keys::PrivateKey::random` + `write_openssh_file` — I want to reuse the exact same approach (via the `russh::keys` crate, already a dependency) to generate the two test SSH keypairs (server_key, client_key) in Rust directly, without shelling out to `ssh-keygen`. Confirm this is feasible and what the minimal code shape looks like (imports, function calls) based on what's already used in that file.
6. Check for a `migrations/` runner already used elsewhere (`sqlx::migrate!` macro usage) I could reuse in a test setup fn.
7. Check if there's any CI config (`.github/workflows/*.yml` or similar) that runs `cargo test` today, and whether it provisions a Postgres service — I need to know if a new DB-dependent integration test would actually run in CI or would need to be gated/ignored.

Report back concisely: what exists, what's reusable, and any blockers/gotchas for building this test (e.g. global state, port binding issues, migration path). Include file:line references.