In repo /home/user/git/luckydonald/tunnel2tunnel, find and report file:line locations for these areas (read enough context to describe current implementation, don't just grep):

1. SSH-side "chat" feature: where chat messages are relayed between sessions in crates/tunnel2tunnel-ssh/src/lib.rs (or elsewhere) — how a typed message becomes a chat broadcast, how client-side text input is read char-by-char (likely `data()` handler on Handler trait), and where "submit on enter" would need to be implemented. Also find the derpy-mail-mare keepalive ping code (already implemented per todo) for comparison/reuse of formatting patterns.
2. Where chat messages currently lack timestamps — the exact string formatting code for the prefixed message `${sender.name} (${sender.uuid}): ${message}`.
3. Frontend: `StatusDot` component (likely frontend/src/components/StatusDot.vue) — how the ring/dot shape is styled (looking for oval vs circle: width vs height).
4. Frontend: where port badges are rendered for server-side ports/services (likely EntityDetailPage.vue or a ports component) — is there a notification-counter-style badge already, and where would "count of real connected ports" data come from (services[].subscribers[].length?).
5. Where the SSH port-forward remote host is hardcoded to "localhost" — likely in tunnel2tunnel-ssh/src/lib.rs's channel_open_direct_tcpip or tcpip_forward handling, or ip_whitelist/entity_ports. Also find the entity_ports model / PortConfig struct to see if there's a "service name" or "name" field already (e.g. `service_name`) that could be lowercased/camelCased into a hostname.
6. Where the "Ban"/"Trap" time-window rule creation validates rules (crates/tunnel2tunnel-core/src/ip_whitelist.rs or a settings/rules page) — specifically anything preventing/allowing a rule that would immediately trap/ban on the very first failed login attempt, including password-attempt-stage failures.
7. `PubkeyInput`/EntitySummary-like collapsed-details component (frontend) referenced by todo item "Clicking the green `-L` command... expand summary to show name field" — find where the collapsed summary/details element lives (likely EntityDetailPage.vue port row or ServiceConnector.vue).

For each area, report: exact file path, line numbers, and a short description of current behavior/data available. Do NOT propose fixes — this is pure research to hand off to planning. Keep total report under 2500 words, organized by numbered section matching above.