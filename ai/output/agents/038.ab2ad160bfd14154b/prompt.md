In this Rust repo, find the `tunnel_e2e` test (recent commits mention "Strengthened the tunnel_e2e test with varied HTTP requests through the tunnel and a second server entity to verify routing"). It's likely in crates/tunnel2tunnel-ssh/tests/ or similar. I need to understand:

1. Exact file path and how the test sets up: DB pool/migrations, SSH server startup (port, host key), entity/user/port_config/subscription/access fixtures created via SQL or model helpers, and how it drives an actual SSH client (russh client? raw TCP? shelling out to `ssh`/openssh binary?) to open a direct-tcpip channel.
2. How it verifies success — does it check bridged data flows correctly? Any assertions on timing?
3. Whether the test ever issues TWO concurrent/overlapping channel-open requests on the SAME ssh connection (this matters — I'm investigating why this test suite didn't catch a bug where one channel-open request blocking for 12s on a `tokio::time::sleep` retry loop inside the `channel_open_direct_tcpip` Handler method serializes ALL other channel activity on that connection, because russh processes each connection on a single task).
4. Any test helper functions for creating a "server never registers" scenario (i.e., a port_config + subscription exist and access is granted, but no entity ever calls tcpip_forward to register the server_slot) — used to test the "target server never came online within the retry window" rejection path (crates/tunnel2tunnel-ssh/src/lib.rs:1235-1243).
5. What test utilities exist for spawning a second concurrent SSH client task, or issuing a second channel_open_direct_tcpip on an existing client's SSH handle/session while a first request is still pending.

Report file paths, function/helper names, and relevant code snippets so I can write a new test that opens two direct-tcpip channels on ONE ssh connection — one to an offline target (forces the 12s timeout) and one to an online target — and asserts the online one's bridge completes quickly (not blocked ~12s behind the offline one). Under 400 words plus code snippets.