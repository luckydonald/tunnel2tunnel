In this Rust+Vue repo (tunnel2tunnel, an SSH tunnel rendezvous manager), I'm planning a feature: add a "Trap" vs "Ban" action to the tarpit threshold rules (currently in `tarpit_thresholds` table: fail_count, window_seconds, enabled — see migrations/009_tarpit_thresholds.sql and crates/tunnel2tunnel-core/src/models/tarpit_threshold.rs), where "Ban" means instantly reject/block the connection (no tarpit games), and "Trap" means the existing tarpit behavior (banner_drip/slow_auth/fake_shell). Also need to record, per connection_logs row, which of three decisions applied: Trap (existing tarpit_method column), auto-Ban (from a threshold rule configured as Ban action), or Admin Ban (a human clicked "Ban" in the admin UI) — and for the latter two, record which admin user (for Admin Ban) or which threshold rule (for auto-Ban) caused it.

Please investigate and report, with exact file paths/line numbers and quoted code:

1. Full current schema/model of `tarpit_thresholds` (migrations/009_tarpit_thresholds.sql, crates/tunnel2tunnel-core/src/models/tarpit_threshold.rs) and `ban_rules` (migrations/008_tarpit.sql's ban_rules section, crates/tunnel2tunnel-core/src/models/ban_rule.rs) — full content of both model files.
2. The in-memory tarpit engine's ban-trigger logic in crates/tunnel2tunnel-ssh/src/tarpit/mod.rs — specifically `record_failure`, `TarpitEntry`, `ThresholdConfig`, `decide_pre_auth_tarpit`, `decide_in_auth_tarpit`, and how `TarpitMethod`/round-robin works. Quote the full file if it's not huge, otherwise the key functions with line numbers.
3. connection_logs full current schema (all migrations touching it: 004, 008, 010) and the full `ConnectionLog` struct + `create()` signature in crates/tunnel2tunnel-core/src/models/connection_log.rs.
4. How the admin "Ban" button currently works end to end: `AdminConnectionLogsPage.vue`'s ban modal/`confirmBan`/`openBanForm` (quote the relevant script section), `adminApi.createBanRule`, and the backend `create_ban_rule`/`CreateBanRuleBody`/`validate_create_ban_rule` in crates/tunnel2tunnel-web/src/routes/tarpit.rs. Also how `AdminUser` extractor works (crates/tunnel2tunnel-web/src/extractors.rs or wherever it's defined) — I need to know what admin-user info (id, username) is available in a route handler.
5. How `ban_rules` rows get merged into the in-memory tarpit ban state (`refresh_once` in tarpit/mod.rs) and how `decide_pre_auth_tarpit`/`decide_in_auth_tarpit` currently always resolve to a `TarpitMethod` (BannerDrip/SlowAuth/FakeShell) for anything banned — I need to understand exactly where "banned" currently unconditionally means "run a tarpit method" so I can see where a hard-reject path would need to branch in instead.
6. The full `T2tHandler`/`T2tServer` accept-loop and auth handler code path in crates/tunnel2tunnel-ssh/src/lib.rs that consumes `decide_pre_auth_tarpit`/`decide_in_auth_tarpit` and calls `Auth::Reject`/`Auth::Accept` — I need to know exactly where a "hard ban, just reject immediately, no tarpit" branch would need to be inserted for both the pre-auth (raw socket) and in-auth (already in a Handler callback) cases.
7. `AdminBanRulesPage.vue` full content (already read once this session, but re-read fresh) so I know the current threshold-rules table markup I'd be extending with an action column.

This is investigation only — do not propose a design or write code, just report facts with file:line references and quoted code so I can design the plan myself.