In tunnel2tunnel repo, frontend/src/labels.ts and any Vue components (grep for `remote_status`, `subscription_live`, `SubscriptionLiveStatus`, "green", "available", tooltip text) that render the subscription "dot"/"ring" live status indicators for a subscriber's own subscription list (e.g. in EntityDetail page or a shared component). Also check frontend/src/pages/EntityDetailPage.vue around where subscriptions render status badges/tooltips, and any component like StatusBadge or LiveStatusDot. Report exact user-facing text/tooltip strings currently shown for: (a) remote_status green/yellow/red states, (b) the `live` boolean dot. Include file:line citations. Keep it short - just the strings and where they live, no analysis needed.