Find frontend Vue code for tarpit_thresholds / ban_rules rule creation UI. Look for components/pages handling "global" rules vs "ip/user" (per-IP or per-user) rule creation forms — likely two separate forms or components that create similar rule objects but bound to different scope/pre-conditions (global vs ip/user). Search frontend/src for keywords: tarpit, ban_rule, threshold, global, scope, ip_whitelist related rule creation, AdminUsers, Settings pages. Report file:line locations of both the global rule creation form/component and the ip/user rule creation form/component, plus their relevant type/interface definitions (in api/admin.ts or similar) and how they differ in fields/props.