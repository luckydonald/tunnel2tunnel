Repo: tunnel2tunnel (Rust backend crates/tunnel2tunnel-web, tunnel2tunnel-ssh, tunnel2tunnel-core; Vue frontend in frontend/).

Bug context: recent commits added realtime WebSocket "live status" for entity ports/subscriptions (status badges, admin Live Connections). Now: a client entity ("linux-laptop") shows in its own debug data (frontend EntityDetail) that a subscribed service's `remote_status` is "green" and it believes the port is available, but the actual owning server entity ("server1")'s debug data shows `live.services[0].live: false` and `subscribers: []` (empty) for that exact port_config_id, even though there IS an active subscription row (`<redacted-uuid>`) linking client entity <redacted-uuid>... to that port_config_id <redacted-uuid>.

I need to understand:
1. Where/how is `live.services[].subscribers` computed for the EntityDetail debug endpoint (or whatever endpoint generates that "copy debug data" JSON) — search for `subscribers`, `remote_status`, `live` fields in Rust backend code (routes, models) and in frontend api/entities.ts or debug-data generation code.
2. Where/how is `remote_status`/`live` on the subscription side (client's view) computed/updated — is it driven by websocket push from server side, or independently computed from DB state?
3. Is there an in-memory (non-DB) state (e.g. `Arc<Mutex<HashMap...>>` slot map mentioned in CLAUDE.md for SSH tcpip_forward) that tracks "is this port actually forwarded right now", separate from the DB subscription row? Find where that slot map is populated/read, and how it relates to the `live` boolean and `subscribers` list.
4. Find the websocket broadcast code (from recent commits about realtime status badges) - how does it decide when to mark a service/subscription as live/not live, and does it correctly notify/update both sides (owner side subscribers list AND subscriber side remote_status) consistently?

Report back: file paths and line numbers for all the above, and a clear description of the actual data flow / state machine for "is a port live", specifically pointing out any place where the owner side and subscriber side could disagree (one shows green/live, other shows not live/empty subscribers) due to the update being one-directional, debounced, or gated on a condition that differs between the two views. Don't propose a fix, just report the mechanics with file:line citations.