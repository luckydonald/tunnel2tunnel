Repo: /home/user/git/luckydonald/base. Read /home/user/.claude/plans/quirky-moseying-wilkinson.md in full for the confirmed design (trailer schema, ref namespace, tree-splitting mechanics, commit metadata policy). This is subsystem (A) of that plan: `sync-splits` forward direction (`ai/UNCLEAN/{branch}` → `{branch}` clean, and → `ai/history/{branch}` history).

Existing shared modules to use as-is (read them first, do not modify them):
- `scripts/°base/git/°split_lib/branches.py` — `BranchFormat`, `classify_branch`, `unclean_name(base)`, `history_name(base)`, `detect_main_branch(repo_root)`.
- `scripts/°base/git/°split_lib/classify.py` — `is_ai_base_path(path)`, `classify_commit(sha, subject, paths) -> CommitClassification` with `.is_ai_only_commit`/`.is_ai_tainted_commit`/`.is_code_containing_commit`.
- `scripts/°base/git/°split_lib/git_ops.py` — subprocess glue, now includes: `repo_root`, `rev_exists`, `rev_parse(ref, cwd) -> str|None`, `rev_list_reverse(range_expr, cwd)`, `changed_paths_for_commit`, `subject_for_commit`, `commit_message(sha, cwd)` (full %B), `tree_for_commit(sha, cwd)`, `create_branch(ref, at_sha, cwd)`, `move_ref(ref, new_sha, old_sha, cwd)`, `commit_tree(tree_sha, parents, message, cwd, *, author_name, author_email, author_date, committer_name, committer_email, committer_date) -> sha`.
- `scripts/°base/git/°split_lib/tree_ops.py` — `build_filtered_tree(parent_target_tree, sha, cwd, *, keep: Callable[[str], bool]) -> new_tree_sha`. This already handles per-commit delta filtering including boundary-crossing renames (decomposes renames into independent delete-of-old/add-of-new, each filtered by `keep`). I already smoke-tested this manually and it works correctly — build on it as-is, don't reinvent tree splitting.
- `scripts/°base/git/°split_lib/trailers.py` — `read_trailers(message, cwd) -> dict[str, list[str]]`, `read_trailer_value(message, key, cwd) -> str|None`, `write_trailers(message, trailers: dict[str,str], cwd) -> str`.
- `scripts/°base/git/°split_lib/identity.py` — `BOT_NAME`, `BOT_EMAIL`, `BOT_AUTHOR` constants (committer identity for tool-generated commits).
- `scripts/°base/tests/_git_test_helpers.py` — `git(args, cwd)`, `make_commit(cwd, filename, message, content=None)`, `init_repo(cwd, *, branch="master")`, `ZERO_SHA`. Use these in your tests instead of duplicating them.

Your job — implement exactly ONE new file `scripts/°base/git/°split_lib/sync_splits.py` and ONE new test file `scripts/°base/tests/test_git_split_sync_splits.py`. Do NOT touch `cli.py`, `git_ops.py`, `tree_ops.py`, `trailers.py`, `branches.py`, `classify.py`, or any other existing file — those are being extended/used by parallel work you must not conflict with. If you find you need a new low-level primitive that genuinely doesn't exist in `git_ops.py`/`tree_ops.py`, implement it as a local helper function inside `sync_splits.py` itself rather than editing those shared files.

Per the plan, implement:
- `find_last_synced_source(target_ref: str, cwd: Path) -> str | None` — read the target branch tip's commit message, extract the `X-Base-Split-Source` trailer via `trailers.read_trailer_value`. Return `None` if the ref doesn't exist or has no such trailer.
- `commits_to_replay(unclean_ref: str, last_synced_source: str | None, lower_bound_ref: str, cwd: Path) -> list[str]` — if `last_synced_source` is set, use `git_ops.rev_list_reverse(f"{last_synced_source}..{unclean_ref}")`. If not set (brand-new target branch), bound replay to commits unique to the feature branch: use `git_ops.merge_base(unclean_ref, lower_bound_ref)` as the lower bound (add a small `merge_base(a, b, cwd)` helper to `git_ops.py`... actually per the "don't touch git_ops.py" rule above: check first whether `merge_base` already exists there — if the file already has it by the time you read it, use it; if not, implement the merge-base lookup as a local subprocess call inside `sync_splits.py` rather than editing `git_ops.py`). `lower_bound_ref` is `main_branch` for the clean cursor's brand-new case and `branches.history_name(main_branch)` for history's.
- `ensure_branch_started(ref: str, base_ref: str, cwd: Path) -> str` — if `ref` doesn't exist (`git_ops.rev_parse` returns `None`), create it via `git_ops.create_branch(ref, git_ops.rev_parse(base_ref, cwd), cwd)`. Return the ref's tip sha either way.
- `kind_for(cls: CommitClassification) -> str` — `"history"` if `cls.is_ai_only_commit`, `"code"` if not `cls.is_ai_tainted_commit`, else `"mixed"`.
- `make_split_commit(parent: str, tree: str, source_sha: str, kind: str, extra_trailers: dict[str,str], cwd: Path) -> str` — read source commit's author name/email/date via `git log -1 --format=%an/%ae/%ad --date=raw` (implement as a small local helper, e.g. `_author_info(sha, cwd)`), build the message (`git_ops.commit_message(source_sha, cwd)` as the base subject+body, run it through `trailers.write_trailers` with `X-Base-Split-Source: <source_sha>`, `X-Base-Split-Kind: <kind>`, plus any `extra_trailers`), then call `git_ops.commit_tree(tree, [parent], message, cwd, author_name=..., author_email=..., author_date=..., committer_name=identity.BOT_NAME, committer_email=identity.BOT_EMAIL, committer_date=<current time — see below>)`. For `committer_date`, since the workflow forbids `Date.now()`-style nondeterminism in some contexts but this is a real script (not a workflow script), use `datetime.now(timezone.utc).isoformat()` or `subprocess`-free `time.time()` formatted as a git-acceptable date string (`"%s +0000" % int(time.time())` works fine as `GIT_COMMITTER_DATE`).
- `SyncSplitsResult` frozen dataclass: `branch`, `clean_ref`, `clean_commits_created`, `clean_commits_skipped_ai_only`, `history_ref`, `history_commits_created`.
- `sync_branch(base_branch: str, *, repo_root: Path, main_branch: str, dry_run: bool = False) -> SyncSplitsResult` — orchestrates: resolve `unclean_ref = branches.unclean_name(base_branch)`, `clean_ref = base_branch`, `history_ref = branches.history_name(base_branch)`; `ensure_branch_started` for clean (base=`main_branch`) and history (base=`branches.history_name(main_branch)`); replay clean commits first (skip `is_ai_only_commit`, else build filtered tree with `keep=lambda p: not classify.is_ai_base_path(p)`, make commit, advance clean tip, track `source_sha -> clean_tree_sha` in a local dict as you go); then replay history commits (every commit, `keep=classify.is_ai_base_path`, look up `X-Base-Split-Counterpart-Tree` from the dict you built in the clean pass if present, add as an extra trailer only when present); for `dry_run=True`, still build trees/commits via `commit_tree` (harmless dangling objects, per the plan) but skip the final `git_ops.move_ref` calls and skip `ensure_branch_started`'s ref creation (compute what the base tip would be without creating the ref) — actually simplest: if dry_run, still call `ensure_branch_started` is wrong since that creates a real ref; instead thread `dry_run` through so `ensure_branch_started`'s ref-creation is skipped and its "current tip" is computed as `git_ops.rev_parse(ref, cwd) or git_ops.rev_parse(base_ref, cwd)` without calling `create_branch`. Return a `SyncSplitsResult` with accurate counts either way.
- `discover_unclean_branches(cwd: Path) -> list[str]` — `git for-each-ref --format=%(refname:short) refs/heads/ai/UNCLEAN/`, strip the prefix via `branches.base_name_from_unclean`.

Tests (`test_git_split_sync_splits.py`), using `_git_test_helpers`, covering exactly the scenarios listed in the plan's test section for (A): pure-code commit, pure-ai commit, mixed commit (assert clean/history trees split correctly and trailers are present/correct via `trailers.read_trailers` on the new commits' `git_ops.commit_message`), a boundary-crossing rename, fresh branch creation (assert clean's first commit's parent is main_branch's tip, history's first commit's parent is `ai/history/master`'s tip), idempotent incremental re-run (sync twice, second run only adds the new commit, doesn't duplicate), an all-ai-only branch (clean branch ref created but gets zero additional commits, still equals main_branch's tip), and `dry_run=True` (assert no ref changes occur before/after).

Run `uv run --project scripts/°base python -m unittest scripts.°base.tests.test_git_split_sync_splits -v` yourself and get all tests passing before finishing. Report back a summary of what you implemented and the final test results.