In /home/user/git/luckydonald/base, read scripts/°base/git/°split_lib/history_master.py in full (it's a few hundred lines) and answer precisely, with line numbers:

Context: `update-history-master` (a git-history-splitting CLI tool) drives most of its work through a scratch branch called `_base_split_scratch`. The helper `_checkout_scratch(onto, cwd)` does `git checkout --detach HEAD` then creates/checks out the scratch branch at `onto`. The helper `_cleanup_scratch(cwd)` detaches HEAD again and deletes the scratch branch. These get called once per replay step (cherry-pick, base-merge recreation, etc.) via `_execute_step`.

There's also `_sync_checkout_if_current(ref_short_name, new_sha, cwd)` which re-syncs a checkout ONLY if the ref that was just moved (`main_branch` after a pull, or `history_ref` after the final move) happens to be the currently-checked-out branch. And `_refuse_if_checked_out_dirty(ref_short_name, cwd)` which refuses to move a ref if it's the currently-checked-out branch AND has uncommitted changes.

I need to know:
1. If a user has some unrelated branch checked out (e.g. `master`, or any branch that is neither `main_branch` nor `history_ref`) when they invoke `update-history-master`, and the run actually executes at least one replay step (so `_checkout_scratch`/`_cleanup_scratch` run), does anything in the code path ever check out that original branch again before the command finishes? Trace exactly which functions run, in order, for a plain `update_history_master(repo_root=..., main_branch="master", yes=True)` call that has >=1 step to replay, and state definitively what branch/detached-state HEAD ends up in at the very end.
2. Same question for the `--continue` path (`_do_continue`) and the `--abort` path (`_do_abort`) — do either of these restore an original checkout, or do they also leave HEAD wherever the scratch-branch cleanup left it?
3. Is there any existing test that checks out a branch OTHER than `main_branch` or `history_ref` (e.g. some third unrelated branch, or nothing at all / already detached) before calling `update_history_master`, and then asserts what branch/HEAD state exists afterward? Search test_git_split_history_master.py and test_git_split_recovery.py.
4. Quote the exact current bodies of `_checkout_scratch`, `_cleanup_scratch`, `_sync_checkout_if_current`, `_refuse_if_checked_out_dirty`, `_do_abort`, `_do_continue`, and the tail of `update_history_master` (the part after `_run_steps` succeeds, through the final return) so I have exact current code to design a fix against.
5. Is there a place where "the branch checked out when the command started" could be captured (e.g. via `git symbolic-ref --short -q HEAD`, which returns empty/nonzero if already detached) once at the very top of `update_history_master`, before `_do_abort`/`_do_continue`/the fresh-run path branch? Would this same captured value need to be persisted into the JSON state file (`_write_state`) for a later separate `--continue`/`--abort` invocation (a different process) to also know what to restore to?

This is research only — do not modify any files. Report exact code quotes and line numbers for everything above.