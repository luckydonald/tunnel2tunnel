Repo: /home/user/git/luckydonald/base — a reusable git "base" template repo. Phase 1 of a branch-splitting feature is already implemented: `scripts/°base/git/°split_lib/` has `branches.py` (branch classification: `clean`={branch}, `unclean`=`ai/UNCLEAN/{branch}`, `history`=`ai/history/{branch}`, plus `classify_branch`, `unclean_name`, `history_name`, `base_name_from_unclean/history`, `detect_main_branch`), `classify.py` (`is_ai_base_path(path)` — true for `ai/**`, `.claude/**`, `.codex/**`, `.mcp.json`, `AGENTS.md`, `CLAUDE.md`, or any path with a `°base` segment; `classify_commit(sha, subject, paths) -> CommitClassification` with `is_ai_only_commit`/`is_ai_tainted_commit`/`is_code_containing_commit`), `git_ops.py` (subprocess glue: `repo_root`, `rev_exists`, `commits_new_to_remote`, `changed_paths_for_commit`, `subject_for_commit`), `push_checks.py` (pure push policy), `cli.py` (argparse, currently only a `check-push` subcommand). Read these files in full before designing — you will extend `cli.py` with a new subcommand and add new modules alongside the existing ones, reusing `branches`/`classify`/`git_ops` rather than duplicating logic.

Design ONLY the `sync-splits` subcommand's **clean** and **history** generation (i.e., unclean → clean, unclean → history). Do NOT design unclean reconstruction (clean+history → unclean) or update-history-master/rebase-branches-to-master — those are being designed by sibling agents in parallel.

Original spec (`ai/°base/todo.md:105-119`):
> Generating clean: Take unclean and strip all AI content, and outright drop ai-only commits. A branch's clean branch will start on the `master` branch, and add commits to that.
> Generating history: Take unclean and strip all code content, but keep the commits even if empty. Add metadata to commits or add specifically crafted metadata commits to store everything needed to sync a clean branch with a history branch back to unclean. A branch's history branch will start on `ai/history/master`, and add commits to that.

Confirmed design decisions from the user (carry these forward, do not re-derive):
- Commit correlation mechanism: git trailers referencing the source commit, e.g. `X-Base-Split-Source: <unclean-sha>` plus a kind marker `X-Base-Split-Kind: code|history|mixed` (user also said "dedicated metadata commits could make sense, too" as a possible supplement — you may propose where those are needed, e.g. to record the exact unclean-branch commit a history branch starts from, since a single trailer per commit doesn't capture "which base commit this whole history branch stems from").
- "Never destructively drop ai: commits" is a strong existing repo norm (see `ai/°base/memory/feedback_lplp_never_drop_ai_autocommits.md`) — for **clean**, "drop ai-only commits" must mean *excluded from clean*, not deleted anywhere; they still exist on `unclean` and are preserved on `history`.
- There is NO existing precedent in this repo for partial-tree commit construction (no `commit-tree`/`read-tree`/`mktree`/`checkout-index` usage anywhere) — you'll need to design this from scratch. The closest existing pattern is `scripts/°base/ai/hooks/_lib.py:180-202`'s `git hash-object -w` + `git update-index --cacheinfo <mode>,<blob>,<path>` blob-staging idiom for single files — read that function (`_restore_staged`) for the exact commands/flags used, since Phase 2 should follow the same low-level git plumbing style rather than introducing an unfamiliar approach.
- No `--allow-empty` precedent exists either, but the spec explicitly requires history commits to persist "even if empty" — design how you construct an empty-tree-diff commit (same tree as parent) via commit-tree, and confirm `git commit-tree` with an identical tree to the parent works fine for this (it does — git allows this at the plumbing level even though `git commit` normally refuses without `--allow-empty`).

What your plan must cover in detail:
1. **Tree-splitting algorithm**: for a given unclean commit, given its full changed-paths list (from `git_ops.changed_paths_for_commit` + full tree lookup, not just changed paths — you need the *complete* tree state at that commit, filtered by `classify.is_ai_base_path`), how do you construct a new tree containing only the code paths (for clean) or only the AI/base paths (for history)? Concretely: do you build the tree incrementally by walking the *parent clean/history commit's tree* and applying only the filtered diff (add/modify/delete only classify-matching paths), using `git diff-tree` per-commit deltas rather than full-tree snapshots? This matters because unclean commits are being replayed onto a *different* base (`master` for clean, `ai/history/master` for history) whose tree doesn't match unclean's tree at all — so you can't just filter unclean's full tree, you need to apply filtered per-commit deltas onto the evolving clean/history tree. Design the exact sequence of git plumbing commands (`git diff-tree -r --no-commit-id --name-status`, `git cat-file`, `git update-index --cacheinfo` / `--index-info`, `git write-tree`, `git commit-tree`) needed to do this correctly, including handling renames/deletes, not just adds/modifies.
2. **Commit-by-commit replay loop**: walk unclean's commits from where clean/history last synced (need a "last synced unclean sha" pointer — design how this is tracked, e.g. via the trailer on the most recent clean/history commit, or a ref like `refs/base-split/<branch>/clean-cursor`) up to unclean's tip, in order, applying the filtered delta to each target branch.
3. **Clean generation**: skip (don't create a commit for) any unclean commit that is `is_ai_only_commit` per `classify.classify_commit`. For commits that are mixed or pure-code, create a new commit on `clean` with the code-only tree, an `X-Base-Split-Source` trailer, and the original subject/author/date preserved (only body/trailers change) — decide exactly which metadata (author, committer, date, subject, body) to preserve vs. regenerate and why.
4. **History generation**: for EVERY unclean commit (including pure-code ones), create a corresponding commit on `history` with the AI/base-only tree (identical to parent's tree, i.e. empty diff, if the source commit was pure-code) and an `X-Base-Split-Source` trailer + `X-Base-Split-Kind`. Design what "metadata to sync clean back with history" means concretely — e.g. does a history commit for a code-only source need a trailer recording that its clean counterpart's tree hash was X, so future unclean-reconstruction can verify commits line up? Propose the exact trailer schema (list every trailer key, its value format, and which commits get which trailers).
5. **New branch creation**: when clean/history don't exist yet for a base branch name, how do you create them (base commit to start from — `master`'s current tip for clean, `ai/history/master`'s current tip for history) before beginning replay.
6. **Idempotency / re-running sync-splits**: if `sync-splits` is run again after new unclean commits are added, it must resume from the cursor, not replay from scratch. Design the cursor-tracking mechanism concretely (ref name/format, or trailer-scan-based lookup, and its performance/correctness tradeoffs).
7. **CLI shape**: exact argparse subcommand name/flags added to `cli.py`, and new module(s) added to `°split_lib/` (e.g. `tree_split.py` for plumbing, `sync_splits.py` for the orchestration), matching the existing file-per-concern style.
8. **Tests**: describe what a real-temp-git-repo test suite for this should cover (following the existing `test_git_split_push_checks.py` end-to-end pattern — real repo via `subprocess`+`tempfile.TemporaryDirectory()`), listing concrete scenarios: pure-code commit, pure-ai commit, mixed commit, a rename that crosses the AI/code boundary (e.g. a file moved from `src/x.py` to `ai/x.py` — is that a deletion+add or something trickier for tree-splitting?), re-running sync-splits incrementally.

Produce a detailed, concrete plan: exact function signatures, exact git command sequences (argv-level, not just names), file paths, and flag any genuinely unresolved edge case you can't confidently resolve rather than silently guessing.