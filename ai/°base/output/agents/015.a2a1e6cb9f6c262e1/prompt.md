Repo: /home/user/git/luckydonald/base. We just implemented a git branch-splitting feature under `scripts/°base/git/°split_lib/` (clean/`ai/UNCLEAN/{branch}`/`ai/history/{branch}` variants, plus `update-history-master`/`rebase-branches-to-master`). Now the user wants a "phase 3": a way to bootstrap the split workflow for a branch that currently exists ONLY as a plain `{branch}` (clean-format) branch with real commits — no `ai/UNCLEAN/{branch}` and no `ai/history/{branch}` exist yet at all. I suspect the existing reverse-direction reconstruction code might already handle most of this if we just create an empty `ai/history/{branch}` first, but I need to verify exactly what's missing.

Please read in full and report back (read-only, no modifications):

1. `scripts/°base/git/°split_lib/sync_unclean.py` — the full file. Specifically trace through `reconstruct_unclean(base_branch, ...)`: what exactly happens when `history_tip` is `None` (history branch doesn't exist) but `clean_tip` is not `None`? Does `_new_shas_since_cursor` handle a `None` tip gracefully? Does `bucket_commits` correctly bucket all of clean's commits as unmatched/code-only when there are zero history commits to correlate against? Where/how does the function decide the base commit to create `ai/UNCLEAN/{branch}` from when it doesn't exist yet (search for `git_ops.create_branch(unclean_ref, ...)` and trace what sha is passed as the base)?
2. Search for "history-master-fork-point" or `refs/base-split/history-master-fork-point` across the whole repo (`scripts/°base/git/°split_lib/*.py`, tests, and `ai/°base/plans/`). Per the Phase 2 plan (`ai/°base/plans/027_*.md`), this ref was supposed to record which `ai/history/master` commit a branch's `history` branch forked from, written once when `history` is first created — confirm whether ANY code actually writes this ref today (check `sync_splits.py`'s `ensure_branch_started`, and `history_master.py`), or whether it's a documented-but-unimplemented gap.
3. `scripts/°base/git/°split_lib/sync_splits.py` — full file, specifically `ensure_branch_started`: when the `history` branch doesn't exist yet, what sha does it use as the starting point, and does it write any bootstrap metadata (fork-point ref or otherwise)?
4. `scripts/°base/git/°split_lib/branches.py` and `history_master.py` — just enough to confirm how `ai/history/master` itself is created/detected (`detect_main_branch`, `history_name`), since a bootstrap command will need to ensure `ai/history/master` already exists before it can fork a per-branch `history` branch from it.
5. Read `ai/°base/plans/027_git-branch-split-phase-2-sync-splits-unclean-reconstruction.md` in full for the confirmed design context (trailer schema, ref namespace, ordering rules) so a follow-up plan for the bootstrap command can stay consistent with it.
6. Check `scripts/°base/tests/test_git_split_sync_unclean.py` for any existing test scenario that already exercises "history missing entirely, only clean exists" — confirm whether this exact scenario is tested today or is a genuine gap.

Report concisely with file:line citations, and be explicit about which of the following it is: (a) fully already works via existing `reconstruct_unclean` once history is created empty, (b) partially works but has bugs/gaps, or (c) doesn't work at all and needs new code. This is research only — do not modify anything.