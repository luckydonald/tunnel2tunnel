Execute this task exactly as a real user-facing coding assistant would.

Skill path: /home/user/git/luckydonald/base/ai/skills/coolify-compose-deploy/SKILL.md — read this file (and its references/ subfolder if relevant) before responding, and follow its guidance.

Task: "Coolify says my whole stack is unhealthy after a deploy, but I think it's just my one-shot Postgres migration container finishing and exiting normally that's causing it. How do I fix this in my docker-compose.coolify.yml?"

Input files: none.

Produce your final answer text (explanation + any compose snippet) and write it to /home/user/git/luckydonald/base/ai/skills/coolify-compose-deploy-workspace/iteration-1/migration-healthcheck/with_skill/outputs/response.md exactly as you'd present it to the user.

Return the final response text as your result too.