Repo: /home/user/git/luckydonald/base — a reusable git "base" template repo. Phase 1 of a branch-splitting feature is already implemented: `scripts/°base/git/°split_lib/` has `branches.py` (branch classification: `clean`={branch}, `unclean`=`ai/UNCLEAN/{branch}`, `history`=`ai/history/{branch}`), `classify.py` (`is_ai_base_path(path)`, `classify_commit(sha, subject, paths) -> CommitClassification` with `is_ai_only_commit`/`is_ai_tainted_commit`/`is_code_containing_commit`), `git_ops.py` (subprocess glue), `push_checks.py`, `cli.py` (argparse, currently only `check-push`). Read these files in full before designing.

Design ONLY the **unclean reconstruction** direction of `sync-splits`: given a `clean` branch and a `history` branch (both derived from some prior `unclean`, or possibly hand-edited independently by a human/reviewer after `clean` was pushed for review), reconstruct/update the `unclean` branch by merging commits from both back together. Do NOT design the unclean→clean/history direction (a sibling agent is designing that — but assume it exists and produces commits carrying trailers `X-Base-Split-Source: <original-unclean-sha>` and `X-Base-Split-Kind: code|history|mixed`, both embedded via `git interpret-trailers`, so you have a way to correlate a clean or history commit back to the unclean commit it was originally derived from). Do NOT design update-history-master/rebase-branches-to-master — a third sibling agent is designing those.

Original spec (`ai/°base/todo.md:120-138`):
> Generating unclean: This is the most difficult one, as you need to merge clean and history back.
> Steps: 1. Start from `history/master` (the specific commit of history/master the branch's history is based on, so all previous AI stuff is contained). 2. Cherry-pick the commits from clean and history in order.
> Three commit categories: (1) code only — exists only in clean, no matching history (e.g. a quick hotfix commit added after the last sync, or AI wasn't needed). (2) history only — exists only in history, no matching clean commit (e.g. a CLAUDE.md update with no code changes). (3) code + history — exists in both, mergeable into a single commit (e.g. a prompt/query file update + the actual implementation).
> A branch's unclean branch starts on `ai/history/master`, and adds commits to that.

Key design tension to resolve: the *forward* direction (unclean→clean/history, being designed by a sibling agent) tags every generated commit with `X-Base-Split-Source: <unclean-sha>`. But this *reconstruction* direction needs to handle THREE cases:
(a) A clean/history commit still carries a valid `X-Base-Split-Source` trailer pointing at an unclean commit that's unchanged — this is the "nothing to do, already reconciled" case, or if unclean's commit content differs from what clean+history would reconstruct (e.g. someone amended the clean branch directly during code review), it's a "clean/history diverged from unclean, need to update unclean" case.
(b) A clean commit or history commit has NO `X-Base-Split-Source` trailer at all (or one pointing at a sha that no longer exists on unclean) — meaning it's genuinely new content added directly to clean or history without going through unclean (spec's "code only"/"history only" categories — someone committed straight to clean or straight to history).
(c) A commit pair (one on clean, one on history) share the same `X-Base-Split-Source` value — these should be merged back into a single unclean commit combining both trees (spec's "code + history" category).

What your plan must cover in detail:
1. **Correlation/matching algorithm**: given the full commit list of `clean` (since it diverged from `master`) and `history` (since it diverged from `ai/history/master`), how do you build the ordered merge sequence? Design the exact grouping logic: parse trailers via `git log --format=%H%x00%B -z` or `git interpret-trailers --parse`, bucket by `X-Base-Split-Source` value (including "no trailer" as its own bucket keyed by sha of the commit itself), and determine a total order across both branches for cases where sources don't correlate 1:1 (e.g. what if commit ordering diverges — clean commit A' logically corresponds to unclean-source X which came before unclean-source Y, but on the history branch the corresponding commit for Y appears before X due to independent edits — is that even possible, and if so how do you detect/reject it rather than silently producing a nonsensical unclean history?).
2. **Merging a code+history pair into one unclean commit**: given clean commit C (tree = code subset) and history commit H (tree = AI/base subset), both referencing the same original unclean source, construct a new unclean commit whose tree = C's changed paths + H's changed paths applied as a delta onto the previous unclean tip. Design the exact git plumbing sequence (this mirrors the sibling agent's forward-direction tree-splitting work, but in reverse — coordinate on reusing the same plumbing-level helper module, e.g. propose it live in a shared `tree_split.py`/`tree_merge.py` so both directions share code rather than duplicating). What commit metadata (author/committer/date/subject/body) does the merged commit use — clean's, history's, or a synthesized combination? What happens if clean's subject and history's subject differ (e.g. clean was manually edited/reworded during review)?
3. **Handling genuinely new commits with no trailer** (case b above): code-only commits get cherry-picked onto unclean as-is (their tree already only touches code paths, so no splitting needed); history-only commits similarly. Design detection of "new" vs "already applied" (a naive cherry-pick could double-apply commits that were already reconstructed in a previous run) — likely needs the same "last-synced cursor" concept the forward direction uses, applied per-branch (a clean-side cursor + a history-side cursor recording how far reconstruction has progressed).
4. **Detecting divergence** (case a's "clean/history changed from what unclean produced"): if unclean already has a commit whose trailer says it's the source for clean-commit C, but C's current tree no longer matches what a fresh split of that unclean commit would produce (someone amended C directly, e.g. during code review feedback), how should reconstruction react — overwrite unclean's corresponding commit (rebase/amend it), add a new "sync-back" commit on top, or refuse and require `--force`/manual resolution? Recommend one approach with reasoning, and flag if you think this needs explicit user confirmation via a CLI flag rather than a fully automatic default.
5. **Ordering strategy**: given the three bucket types can be interleaved arbitrarily in real history, propose the concrete sort key used to walk them into one linear unclean sequence (e.g. sort by: the position of the `X-Base-Split-Source` sha in the ORIGINAL unclean branch's ancestry for matched commits, and by commit date/branch-position for unmatched "new" commits — but be explicit, this is exactly the ambiguity the user flagged as "the most difficult one").
6. **Starting point**: confirm/expand on "starts from the specific commit of `ai/history/master` the branch's history is based on" — how is that specific commit identified concretely (a trailer or ref stored when `history` branch was first created, pointing back to the `ai/history/master` commit it forked from)?
7. **CLI shape**: what subcommand/flags does this need in `cli.py` (likely part of the same `sync-splits` subcommand as the forward direction, operating bidirectionally, or a distinct mode flag — recommend one).
8. **Tests**: concrete real-temp-git-repo scenarios covering all three commit categories plus the divergence-detection case and idempotent re-running.

Produce a detailed, concrete plan. Flag any genuinely unresolved ambiguity (especially around ordering/divergence) rather than silently picking an answer you're not confident in — this is explicitly the hardest part of the whole feature per the user's own spec.