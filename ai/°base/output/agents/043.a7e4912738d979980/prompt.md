In the repo /home/user/git/luckydonald/base, the file `ai/°base/query.md` is a large (4000+ line) append-only transcript log of past prompts/decisions/plans in this repo. There is a `code-style` skill at `ai/skills/code-style/` with `SKILL.md` (frontmatter: `description: "... Detect the languages used by the files in scope and load only their matching references; Languages: py."`) and one reference file so far: `ai/skills/code-style/references/py.md`.

Task: search all of `ai/°base/query.md` for places where the user stated a coding-style constraint/preference/convention for ANY programming language — things like "always use native types instead of typing.*", "prefer f-strings", "use tabs not spaces", "end every class/def with a comment", indentation rules, naming conventions, import ordering, error-handling style, etc. — for Python AND any other language (JavaScript/TypeScript, Bash/shell, Go, Rust, SQL, YAML, Docker, whatever appears). These are typically short asides embedded in otherwise unrelated prompts, not necessarily labeled "style" — look for phrasing like "for X, prefer...", "always...", "never...", "don't use...", "should end with...", etc. tied to a specific language or file type.

Read the existing `ai/skills/code-style/references/py.md` first so you know what's ALREADY captured (don't re-report those) — I need the ones NOT yet in that file, across all languages.

Report back a list grouped by language/file-extension, each entry with: the exact constraint text (verbatim or tightly paraphrased), and the approximate line number/context in `query.md` where it appears (grep line numbers are fine). Do not edit any files — this is read-only research to feed into writing new/updated reference files.