Repo: /home/user/git/luckydonald/base. Read /home/user/.claude/plans/quirky-moseying-wilkinson.md in full for the confirmed design (trailer schema, ref namespace, tree-splitting mechanics, ordering/divergence rules). This is subsystem (B) of that plan: `sync-splits` reverse direction ("unclean reconstruction") — given `{branch}` (clean) and `ai/history/{branch}` (history), reconstruct/update `ai/UNCLEAN/{branch}`.

Existing shared modules to use as-is (read them first, do not modify them — a parallel agent may be extending some of them concurrently, so treat any conflict as YOUR problem to work around locally, not theirs to fix):
- `scripts/°base/git/°split_lib/branches.py` — `BranchFormat`, `classify_branch`, `unclean_name(base)`, `history_name(base)`, `detect_main_branch(repo_root)`.
- `scripts/°base/git/°split_lib/classify.py` — `is_ai_base_path(path)`, `classify_commit(sha, subject, paths) -> CommitClassification`.
- `scripts/°base/git/°split_lib/git_ops.py` — subprocess glue, including: `repo_root`, `rev_exists`, `rev_parse(ref, cwd) -> str|None`, `rev_list_reverse(range_expr, cwd)`, `commit_message(sha, cwd)` (full %B), `tree_for_commit(sha, cwd)`, `create_branch`, `move_ref(ref, new_sha, old_sha, cwd)`, `commit_tree(tree_sha, parents, message, cwd, *, author_name, author_email, author_date, committer_name, committer_email, committer_date) -> sha`, `merge_base(sha_a, sha_b, cwd) -> str|None` (may or may not exist yet — if missing, implement your own local `_merge_base` helper inside `sync_unclean.py` rather than editing `git_ops.py`).
- `scripts/°base/git/°split_lib/tree_ops.py` — `PathChange` dataclass (`status`, `path`, `old_path`), `raw_diff_for_commit(sha, cwd) -> list[PathChange]` (a commit's own delta vs its parent, rename-aware), `apply_path_changes(base_tree, changes: list[PathChange], sha_for_blobs, cwd) -> new_tree_sha` (overlays changes onto a base tree, resolving blobs from `sha_for_blobs`'s tree; handles renames by decomposing into delete-old + add-new). These are exactly the primitives you need for merging a clean commit's delta + a history commit's delta into one combined tree, and for cherry-picking an unmatched commit's delta onto the running unclean tree. I already smoke-tested `tree_ops.py`'s core logic manually — trust it, don't reinvent tree splitting.
- `scripts/°base/git/°split_lib/trailers.py` — `read_trailers(message, cwd) -> dict[str, list[str]]`, `read_trailer_value(message, key, cwd) -> str|None`, `write_trailers(message, trailers: dict[str,str], cwd) -> str`.
- `scripts/°base/git/°split_lib/identity.py` — `BOT_NAME`, `BOT_EMAIL` constants.
- `scripts/°base/tests/_git_test_helpers.py` — `git(args, cwd)`, `make_commit(cwd, filename, message, content=None)`, `init_repo(cwd, *, branch="master")`, `ZERO_SHA`.

Trailer scheme already fixed by the plan (a parallel agent is implementing the forward direction that PRODUCES these — you are the CONSUMER, so build against the schema, not against that agent's code):
- `X-Base-Split-Source: <unclean-sha>` on every generated clean/history commit.
- `X-Base-Split-Kind: code|history|mixed` on every generated clean/history commit.
- `X-Base-Split-Counterpart-Tree: <clean-tree-sha>` on history commits when a clean counterpart exists.

Your job — implement exactly ONE new file `scripts/°base/git/°split_lib/sync_unclean.py` and ONE new test file `scripts/°base/tests/test_git_split_sync_unclean.py`. Do NOT touch `cli.py`, `git_ops.py`, `tree_ops.py`, `trailers.py`, `branches.py`, `classify.py`, `sync_splits.py`, or any other existing/concurrently-created file. Since your tests need clean/history commits carrying the right trailers as fixtures, hand-craft them directly in your test file using `trailers.write_trailers` + `git_ops.commit_tree` (or plain `git commit --trailer "Key: value"` shell calls via `_git_test_helpers.git`) — do NOT depend on importing or calling the forward-direction module, since it's being built concurrently and may not be finished/stable when you run.

Per the plan, implement:
- Ref namespace for cursors: `refs/base-split/unclean-cursor/clean/{branch}` and `refs/base-split/unclean-cursor/history/{branch}` — last clean/history sha successfully incorporated into unclean. Use `git_ops.rev_parse`/`git_ops.move_ref` against these plain refs (not under `refs/heads/`).
- `CommitInfo` dataclass: `sha`, `subject`, `body`, `author_name`, `author_email`, `author_date`, `paths: list[PathChange]` (from `raw_diff_for_commit`), and parsed trailers (`source: str|None`, `kind: str|None`).
- `read_commit_info(sha: str, cwd: Path) -> CommitInfo` — pull message via `git_ops.commit_message`, trailers via `trailers.read_trailers`, diff via `tree_ops.raw_diff_for_commit`, author info via a local helper (git log -1 --format=%an/%ae/%ad --date=raw).
- `bucket_commits(clean_infos: list[CommitInfo], history_infos: list[CommitInfo], cwd: Path) -> dict[str, dict]` — key = `X-Base-Split-Source` value if that sha resolves to a real commit locally (via `git_ops.rev_exists`), else `("unmatched", sha)` (use the commit's own sha as the unique key, e.g. represent as a tuple key). Value = `{"clean": CommitInfo|None, "history": CommitInfo|None}`. If two clean (or two history) commits share the same source key, raise a clear `ValueError` naming both shas (per the plan: refuse rather than guess).
- `order_key(item, unclean_source_order: list[str]) -> tuple` implementing exactly the sort key from the plan: matched items sort by `unclean_source_order.index(key)`; unmatched items sort by `(inf, preceding_matched_index, committer_date_iso, 0_if_clean_else_1)`. Since `unclean_source_order` may be empty (no prior reconstruction / hand-created branches), handle that gracefully (everything falls into the unmatched-tiebreak path, sorted purely by date, clean-before-history on exact ties).
- Divergence check: for a matched key whose source sha is *already* covered by BOTH cursor refs (i.e., previously reconstructed), recompute the combined delta from current clean+history and compare against the recorded unclean commit's own `raw_diff_for_commit` — if they differ (content, not just message), that's divergence. Implement `detect_divergences(...) -> list[dict]` describing each diverged commit (old unclean sha, new expected tree, what changed) WITHOUT modifying anything.
- `build_merged_commit(unclean_prev: str, item: dict, cwd: Path, *, dry_run: bool) -> str` — for a matched pair (clean+history both present): combine `changes_C + changes_H` via `tree_ops.apply_path_changes(unclean_prev_tree, combined_changes, sha_for_blobs=???, cwd)` — NOTE `apply_path_changes` takes one `sha_for_blobs` but you have two source commits (clean's sha for code paths, history's sha for ai paths); since blobs are looked up per-path from a single tree-ish, either (a) call `apply_path_changes` twice in sequence (once with clean's changes against clean's own sha, once with history's changes against history's own sha, chaining the resulting tree each time), or (b) extend the call site to look up each path's blob from whichever commit actually changed it. Recommend approach (a) — sequential application — since `apply_path_changes` already supports incremental overlay (that's exactly what "chain the resulting tree" means: call it once for `changes_C` against `C.sha`, feed the result tree into a second call for `changes_H` against `H.sha`). Metadata resolution per the plan: author name/email/date from `C` (clean) by default; committer = identity bot + current time; subject/body: use `C`'s verbatim if `C.subject != H.subject`, appending non-boilerplate `H` body content (heuristic: non-empty after stripping trailers and matching `classify.AI_SUBJECT_RE`-boilerplate lines — a simple "more than one non-trivial line" heuristic is fine) under a `---` separator; else use `C`'s message as-is. For unmatched-only items (code-only or history-only), it's a straight `apply_path_changes(unclean_prev_tree, item_changes, item_sha, cwd)` with that commit's own author/message verbatim (a true cherry-pick). Build the new unclean commit via `git_ops.commit_tree`. Respect `dry_run` (build tree/commit objects is harmless, just don't `move_ref`).
- `reconstruct_unclean(base_branch: str, *, repo_root: Path, main_branch: str, allow_diverge_rewrite: bool = False, force: bool = False, dry_run: bool = False) -> dict` — top-level orchestration: resolve refs, read cursor positions, gather new clean/history commits since cursors (`git_ops.rev_list_reverse`), bucket + order them, detect divergence among already-cursored matched items (report; only act if `allow_diverge_rewrite`, and if so, amend the corresponding unclean commit in place and replay descendants — implement this as rebuilding the unclean branch's commits from the diverged point forward, using the same `build_merged_commit`-style logic per descendant), then build new unclean commits for the newly-bucketed items in sort order, updating the branch tip and cursor refs at the end (unless `dry_run`). If `order_key` detects a matched-pair whose relative order contradicts a previously-recorded `unclean_source_order`, raise unless `force=True` (fall back to trusting clean's order). Return a result dict with counts (commits created, divergences found/fixed, etc.) for CLI reporting.

Tests (`test_git_split_sync_unclean.py`) covering exactly the scenarios in the plan's (B) test list: code+history pair merge (hand-craft trailers, assert merged tree matches expected), code-only cherry-pick, history-only cherry-pick, divergence detected but not rewritten by default, divergence rewritten with `allow_diverge_rewrite=True`, idempotent re-run (no new commits, cursors/tip unchanged), dangling trailer sha falls back to unmatched, duplicate-trailer collision raises.

Run `uv run --project scripts/°base python -m unittest scripts.°base.tests.test_git_split_sync_unclean -v` yourself and get all tests passing before finishing. This is explicitly the hardest subsystem per the user's own spec — if you hit a genuine design ambiguity beyond what's in the plan, make the most defensible choice, document it clearly in a code comment, and note it in your final report rather than silently guessing forever. Report back a summary of what you implemented, any deviations from the plan you had to make, and the final test results.