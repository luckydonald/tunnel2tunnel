Repo: /home/user/git/luckydonald/base (a reusable "base" template repo merged/rebased into consuming projects; see ai/°base/AGENTS.md / CLAUDE.md for full conventions).

We're designing PHASE 1 (foundation only) of a git branch-splitting feature. The full feature (documented by the user in ai/°base/todo.md lines 59-163, not yet implemented anywhere) introduces three branch variants per feature branch:
- clean: `{branch}` — no AI/base mentions, safe for public/customer release
- unclean: `ai/UNCLEAN/{branch}` — the real working branch, mixed AI+code commits allowed
- history: `ai/history/{branch}` — AI-only leftovers/metadata after code is stripped from unclean

Full phase 2 (sync-splits generation, ai/history/master rebase strategy, rebase-branches-to-master, unclean reconstruction via commit correlation trailers) is EXPLICITLY OUT OF SCOPE for this plan — will be designed later. Do not design those.

PHASE 1 SCOPE (this plan only):
1. **Branch naming/classification helpers** — given a branch name, classify it as clean/unclean/history, and derive unclean-name/history-name from a base branch name, and vice versa (extract base branch name from unclean/history name). Regex: unclean = `^ai/UNCLEAN/(.+)$`, history = `^ai/history/(.+)$`, anything else = clean. Special-case: `ai/history/master` (or whatever the repo's main branch is called) is the history-master branch, distinct from a per-feature history branch — should still classify as "history" type but callers may want to detect the master case too.
2. **AI/base content classifier** — for a given repo-relative path, determine if it's "AI/base content" (must never appear in a clean branch) using this confirmed rule set: path is under `ai/**`, `.claude/**`, `.codex/**`, equals `.mcp.json`, equals `AGENTS.md`, equals `CLAUDE.md`, OR any path component (any single directory segment) exactly equals `°base` (catches `scripts/°base/**`, `ai/°base/**`, and any future `<anything>/°base/**`). For a given commit (identified by paths changed = `git show --name-only`/`git diff-tree`, and subject line), determine:
   - `is_ai_only_commit`: every changed path is AI/base content.
   - `is_ai_tainted_commit` (stricter, used for push blocking): true if `is_ai_only_commit`, OR any changed path is AI/base content (mixed commit), OR the subject matches `/^(\[.*\]\s*)?ai:/` (case-sensitive, matching the repo's real `ai:`/`[base] ... : ai: Run: ...` commit convention — see ai/°base/AGENTS.md commit format docs and scripts/°base/ai/hooks/_lib.py for real examples).
   - `is_code_containing_commit`: true if any changed path is NOT AI/base content (i.e. touches real code/product files). Note a commit can be both ai_tainted AND code_containing (a mixed commit) — that's fine, it's only legal on `unclean`-format branches.
3. **Push-content policy matrix** (confirmed with user):
   | branch format | AI-tainted commits allowed | code-containing commits allowed |
   |---|---|---|
   | unclean | yes | yes |
   | clean | no (block) | yes |
   | history | yes | no (block) |
4. **Push-name policy**: block pushing any branch classified as `unclean` or `history` to a remote literally named `origin` (regardless of content). This is a separate, unconditional check (name-based, not content-based) — and per the user's spec is specifically about the `origin` remote; the content-matrix check in (3) applies on push to ANY remote, not just origin.
5. **A real git pre-push hook** enforcing (3) and (4), wired into this repo's existing hook infrastructure:
   - Existing infra found by research: `.pre-commit-config.yaml` at repo root currently only wires `commit-msg` stage hooks (`scripts/°base/git/hooks/commit/reject_co_authored_by.py`, `require_memory_delete_marker.py`) and a `pre-commit`-stage `ai-settings-sync` hook. There is currently NO pre-commit-managed `pre-push` stage at all — `.git/hooks/pre-push` today only runs `git lfs pre-push "$@"`.
   - Installer: `scripts/°base/git/hooks/install/__init__.py` (`_install_hooks()`, calls `pre_commit install --hooks-type commit-msg` at line ~63) and the parallel `install/setup.py` (same, line ~29) currently only install the `commit-msg` hook type — both need a second `pre_commit install --hooks-type pre-push` call so `.git/hooks/pre-push` becomes a pre-commit-framework dispatcher instead of staying pure git-lfs. Must preserve the existing git-lfs pre-push invocation (pre-commit's generated dispatcher chains local hooks, but confirm how it composes with the pre-existing lfs-only script — may need pre-commit's `default_install_hook_types` merge behavior investigated, or the new hook script itself should shell out to `git lfs pre-push "$@"` first if pre-commit fully replaces the file).
   - A pre-push hook script receives `<remote name> <remote url>` as argv, and ref-update lines on stdin: `<local ref> <local sha> <remote ref> <remote sha>` (one per line, multiple lines possible for `git push` pushing several branches/tags at once). Need to design the parsing and the per-ref-line application of rules (3)+(4), aggregating any violations into one clear, actionable error message (listing every offending ref/commit) before exiting non-zero (which aborts the whole push).
   - For content checks, the script needs to enumerate commits being newly pushed for each ref — i.e. commits reachable from `<local sha>` not already reachable from `<remote sha>` (handle `<remote sha>` all-zeros = new branch case, and deleted-branch case where `<local sha>` is all-zeros = skip). Use `git rev-list <remote_sha>..<local_sha>` (or `--not --remotes=<remote>` style) per ref line.
6. **CLI entry point** (confirmed with user): `scripts/°base/git/split.py` — thin argparse shim delegating to a `scripts/°base/git/°split_lib/` package (mirrors existing `ai/settings/sync.py` → `°settings_lib/cli.py` pattern — read that file for the exact shim style: `main = importlib.import_module("°settings_lib.cli").main; raise SystemExit(main())`). Inside `°split_lib/`:
   - `branches.py` — item 1
   - `classify.py` — item 2
   - `push_checks.py` — items 3+4 policy logic (pure functions, testable without touching git hook plumbing)
   - `cli.py` — argparse with (at least) a `check-push` subcommand that the pre-push hook script invokes, taking `--remote-name`, `--remote-url` and reading ref-update lines from stdin (matching real pre-push hook invocation), calling into `push_checks.py`.
   - The actual pre-push hook script itself: where should it live — a new file under `scripts/°base/git/hooks/push/` (mirroring `hooks/commit/`) that's a thin wrapper calling `split.py check-push "$@"`? Design this file too.
7. **Tests**: this repo uses stdlib `unittest`, dynamic module loading via `importlib.util.spec_from_file_location` (see `scripts/°base/tests/test_git_remote_fix.py` and `test_rebase_strip_claude_authorship.py` for the pattern — real throwaway git repos created via `subprocess` + `tempfile.TemporaryDirectory()`). Design test file(s) (e.g. `scripts/°base/tests/test_git_split_branches.py`, `test_git_split_classify.py`, `test_git_split_push_checks.py`) covering: branch classification edge cases, AI/base path classification (including the `°base`-segment-anywhere rule), commit classification (ai-only/ai-tainted/code-containing/mixed), and the push policy matrix end-to-end against a real temp git repo (create commits, simulate ref-update lines, assert allow/block + message content).

Also read `scripts/°base/git/rebase_strip_claude_authorship.py` and its test for style/identity-handling precedent (not directly reused here, but keep style consistent), and `ai/°base/memory/feedback_lplp_never_drop_ai_autocommits.md` (via MEMORY.md) — note this only affects phase 2 (dropping ai-only commits when generating clean), not phase 1, but keep in mind the repo's strong norm against destructively discarding `ai:` commits when you note phase-2 hooks for future work.

Produce a detailed, concrete implementation plan: exact file paths, function signatures, the pre-push hook script's exact shell/python content and argv/stdin handling, the `.pre-commit-config.yaml` diff, the installer diff, and a description of each test file's key test cases. Flag any remaining open design questions you find (e.g. exact pre-commit `pre-push` stage composition with the existing LFS-only pre-push script) rather than silently guessing on anything that could break existing git-lfs push behavior.