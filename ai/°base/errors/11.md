❯ > Where is the Thomann orderlist HTML file (`/mythomann_orderlist.html`)? I couldn't find it in the repo.
> - I'll provide it / upload it soon
> - It's on my local machine (not in repo)
>
> For Shein orders: the existing Shein product provider uses Playwright + curl_cffi to bypass anti-bot challenges. The orders list page at de.shein.com/user/orders/list requires login. Which login strategy should we use?
> - Playwright (browser automation)
> - Cookie-based (manual login)
> - httpx with curl_cffi
> → It needs to be fetched from the domains (EN, DE, ...)
>   Try if _`httpx` with `curl_cffi`_, like the **Shein product provider**, is working, otherwise we have to use _`playwright`_ like **AliExpress**.
> ```json
> {
>   "questions": [
>     {
>       "question": "Where is the Thomann orderlist HTML file (`/mythomann_orderlist.html`)? I couldn't find it in the repo.",
>       "header": "HTML file location",
>       "options": [
>         {
>           "label": "I'll provide it / upload it soon",
>           "description": "You don't have it yet — save a fixture placeholder and implement the parser skeleton; the HTML will come later."
>         },
>         {
>           "label": "It's on my local machine (not in repo)",
>           "description": "Tell me the full path and I'll read it directly to understand the structure before writing the parser."
>         }
>       ],
>       "multiSelect": false
>     },
>     {
>       "question": "For Shein orders: the existing Shein product provider uses Playwright + curl_cffi to bypass anti-bot challenges. The orders list page at de.shein.com/user/orders/list requires login. Which login strategy should we use?",
>       "header": "Shein login strategy",
>       "options": [
>         {
>           "label": "Playwright (browser automation)",
>           "description": "Same approach as AliExpress — automate a headless Chrome browser to log in and scrape the orders page. Handles JS-heavy pages and anti-bot naturally."
>         },
>         {
>           "label": "Cookie-based (manual login)",
>           "description": "Reuse the existing Shein session infrastructure (cookies/headers from product_dl) — user logs in manually via browser, exports cookies, and list_dl uses those. Simpler but requires manual intervention."
>         },
>         {
>           "label": "httpx with curl_cffi",
>           "description": "Try direct HTTP requests with Chrome TLS impersonation, similar to how the Shein product provider fetches pages."
>         }
>       ],
>       "multiSelect": false
>     }
>   ],
>   "answers": {
>     "Where is the Thomann orderlist HTML file (`/mythomann_orderlist.html`)? I couldn't find it in the repo.": "It needs to be fetched from the domains (EN, DE, ...)",
>     "For Shein orders: the existing Shein product provider uses Playwright + curl_cffi to bypass anti-bot challenges. The orders list page at de.shein.com/user/orders/list requires login. Which login strategy should we use?": "Try if _`httpx` with `curl_cffi`_, like the **Shein product provider**, is working, otherwise we have to use _`playwright`_ like **AliExpress**."
>   },
>   "annotations": {}
> }
> ```
>
