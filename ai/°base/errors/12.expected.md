# Input JSON (Claude)
```json
{
  "questions": [
    {
      "question": "Which Python version should we target?",
      "header": "Python ver",
      "options": [
        {
          "label": "3.12 (Recommended)",
          "description": "Latest stable release with improved error messages and performance."
        },
        {
          "label": "3.11",
          "description": "Previous stable, widely deployed."
        },
        {
          "label": "3.10",
          "description": "Older but still supported; some environments require it."
        }
      ],
      "multiSelect": false
    },
    {
      "question": "Which testing libraries do you want included?",
      "header": "Test libs",
      "options": [
        {
          "label": "pytest",
          "description": "De facto standard test runner."
        },
        {
          "label": "hypothesis",
          "description": "Property-based testing."
        },
        {
          "label": "coverage.py",
          "description": "Measures test coverage."
        },
        {
          "label": "pytest-asyncio",
          "description": "Async test support for pytest."
        }
      ],
      "multiSelect": true
    },
    {
      "question": "How should errors be surfaced to the caller?",
      "header": "Error style",
      "options": [
        {
          "label": "Exceptions (Recommended)",
          "description": "Raise typed exceptions; caller uses try/except.",
          "preview": "class NotFoundError(AppError):\n    pass\n\ndef get_user(uid: int) -> User:\n    if not found:\n        raise NotFoundError(uid)"
        },
        {
          "label": "Result type",
          "description": "Return Ok/Err wrapper; caller pattern-matches.",
          "preview": "def get_user(uid: int) -> Result[User, str]:\n    if not found:\n        return Err(f\"user {uid} not found\")\n    return Ok(user)"
        },
        {
          "label": "Optional + logging",
          "description": "Return None on failure and log internally.",
          "preview": "def get_user(uid: int) -> User | None:\n    if not found:\n        logger.warning(\"user %s not found\", uid)\n        return None\n    return user"
        }
      ],
      "multiSelect": false
    },
    {
      "question": "Which areas of the codebase are in scope for this change?",
      "header": "Scope",
      "options": [
        {
          "label": "Core library",
          "description": "The main package under src/."
        },
        {
          "label": "CLI",
          "description": "Command-line entry points."
        },
        {
          "label": "Tests",
          "description": "Test suite under tests/."
        },
        {
          "label": "Docs",
          "description": "Documentation and examples."
        }
      ],
      "multiSelect": true
    },
   {
      "question": "Select some numbers (testing order) — first batch:",
      "header": "Numbers 1-4",
      "options": [
        {
          "label": "One",
          "description": "The first."
        },
        {
          "label": "Two",
          "description": "The second."
        },
        {
          "label": "Three",
          "description": "The third."
        },
        {
          "label": "Four",
          "description": "The fourth."
        }
      ],
      "multiSelect": true
    },
    {
      "question": "Pick a logging approach:",
      "header": "Logging",
      "options": [
        {
          "label": "Structured JSON",
          "description": "Machine-readable log lines.",
          "preview": "logger.info(\"user.login\", extra={\n    \"user_id\": uid,\n    \"ip\": request.remote_addr,\n})"
        },
        {
          "label": "Plain text",
          "description": "Human-readable, simple.",
          "preview": "logger.info(f\"User {uid} logged in from {ip}\")"
        },
        {
          "label": "No logging",
          "description": "Silence — caller decides.",
          "preview": "# nothing emitted here"
        }
      ],
      "multiSelect": false
    },
    {
      "question": "Pick a logging approach (one option has no preview):",
      "header": "Logging",
      "options": [
        {
          "label": "Structured JSON",
          "description": "Machine-readable log lines.",
          "preview": "logger.info(\"user.login\", extra={\n    \"user_id\": uid,\n    \"ip\": request.remote_addr,\n})"
        },
        {
          "label": "Plain text",
          "description": "Human-readable, simple.",
          "preview": "logger.info(f\"User {uid} logged in from {ip}\")"
        },
        {
          "label": "No logging",
          "description": "Silence — caller decides. (no preview set for this one)"
        }
      ],
      "multiSelect": false
    }
  ],
  "answers": {
    "Which Python version should we target?": "3.12 (Recommended)",
    "Which testing libraries do you want included?": "pytest, coverage.py, Bananas and Mangos. Best fruits.",
    "How should errors be surfaced to the caller?": "(notes only)",
    "Which areas of the codebase are in scope for this change?": "Docs, CLI, Mangoes. Womengoes?",
    "Select some numbers (testing order) — first batch:": "Four, Two, Three, I will select first the extra option, then four, two, three. Yes yes.",
    "Pick a logging approach:": "No logging",
    "Pick a logging approach (one option has no preview):": "No logging"
  },
  "annotations": {
    "How should errors be surfaced to the caller?": {
      "notes": "This is a random note I wrote (it rhymes!)"
    },
    "Pick a logging approach:": {
      "preview": "# nothing emitted here"
    }
  }
}
```

# `query.md` addition

❯ Question answered.
> <details><summary>
>
>> 1. Which Python version should we target?
>>    - 3.12 (Recommended)
>> 2. Which testing libraries do you want included?
>>    - pytest
>>    - coverage.py
>>    - _Other_: Bananas and Mangos. Best fruits.
>> 3. How should errors be surfaced to the caller?
>>    - _Other_: This is a random note I wrote (it rhymes!)
>> 4. Which areas of the codebase are in scope for this change?
>>    - Docs
>>    - CLI
>>    - _Other_: Mangoes. Womengoes?
>> 5. Select some numbers (testing order) — first batch:
>>    - Four
>>    - Two
>>    - Three
>>    - _Other_: I will select first the extra option, then four, two, three. Yes yes.
>> 6. Pick a logging approach:
>>    - No logging
>>      ```text
>>      # nothing emitted here
>>      ```
>> 7. Pick a logging approach (one option has no preview):
>>    - No logging
>
> (click to expand)
>
> </summary>
>
>> **Python ver** (1/7) <kbd>Single Select</kbd><br>
>> Which Python version should we target?
> - [x] 1\. 3.12 (Recommended)
>   - _Latest stable release with improved error messages and performance._
> - [ ] 2\. 3.11
>   - _Previous stable, widely deployed._
> - [ ] 3\. 3.10
>   - _Older but still supported; some environments require it._
> - [ ] 4\. _Type something._
>
>> **Test libs** (2/7) <kbd>Multi Select</kbd><br>
>> Which testing libraries do you want included?
> - [x] 1\. pytest <sup><sub><kbd>#1</kbd></sub></sup>
>   - _De facto standard test runner._
> - [ ] 2\. hypothesis
>   - _Property-based testing._
> - [x] 3\. coverage.py <sup><sub><kbd>#2</kbd></sub></sup>
>   - _Measures test coverage._
> - [ ] 4\. pytest-asyncio
>   - _Async test support for pytest._
> - [x] 5\. _Type something:_
>   - > Bananas and Mangos. Best fruits.
>
>> **Error style** (3/7) <kbd>Single Select</kbd><br>
>> How should errors be surfaced to the caller?
> - [ ] 1\. Exceptions (Recommended)
>   - _Raise typed exceptions; caller uses try/except._
>   - ```
>     class NotFoundError(AppError):
>         pass
>
>     def get_user(uid: int) -> User:
>         if not found:
>             raise NotFoundError(uid)
>     ```
> - [ ] 2\. Result type
>   - _Return Ok/Err wrapper; caller pattern-matches._
>   - ```
>     def get_user(uid: int) -> Result[User, str]:
>         if not found:
>             return Err(f"user {uid} not found")
>         return Ok(user)
>     ```
> - [ ] 3\. Optional + logging
>   - _Return None on failure and log internally._
>   - ```text
>     def get_user(uid: int) -> User | None:
>         if not found:
>             logger.warning("user %s not found", uid)
>             return None
>         return user
>     ```
> - [x] 4\. _Notes:_
>   - > This is a random note I wrote (it rhymes!)
>
>> **Scope** (4/7) <kbd>Multi Select</kbd><br>
>> Which areas of the codebase are in scope for this change?
> - [ ] 1\. Core library
>   - _The main package under src/._
> - [x] 2\. CLI <sup><sub><kbd>#2</kbd></sub></sup>
>   - _Command-line entry points._
> - [ ] 3\. Tests
>   - _Test suite under tests/._
> - [x] 4\. Docs <sup><sub><kbd>#1</kbd></sub></sup>
>   - _Documentation and examples._
> - [ ] 5\. _Type something:_
>   - > Mangoes. Womengoes?
>
>> **Numbers 1-4** (5/7) <kbd>Multi Select</kbd><br>
>> Select some numbers (testing order) — first batch:
> - [ ] 1\. One
>   - _The first._
> - [x] 2\. Two <sup><sub><kbd>#2</kbd></sub></sup>
>   - _The second._
> - [x] 3\. Three <sup><sub><kbd>#3</kbd></sub></sup>
>   - _The third._
> - [x] 4\. Four <sup><sub><kbd>#1</kbd></sub></sup>
>   - _The fourth._
> - [x] 5\. _Type something:_
>   - > I will select first the extra option, then four, two, three. Yes yes.
>
>> **Logging** (6/7) <kbd>Single Select</kbd><br>
>> Pick a logging approach:
> - [ ] 1\. Structured JSON
>   - _Machine-readable log lines._
>   - ```
>     logger.info("user.login", extra={
>         "user_id": uid,
>         "ip": request.remote_addr,
>     })
>     ```
> - [ ] 2\. Plain text
>   - _Human-readable, simple._
>   - ```
>     logger.info(f"User {uid} logged in from {ip}")
>     ```
> - [ ] 3\. No logging
>   - _Silence — caller decides._
>   - ```text
>     # nothing emitted here
>     ```
> - [ ] 4\. _Notes: Add notes on this design._
>
>> **Logging** (7/7) <kbd>Single Select</kbd><br>
>> Pick a logging approach (one option has no preview):
> - [ ] 1\. Structured JSON
>   - _Machine-readable log lines._
>   - ```
>     logger.info("user.login", extra={
>         "user_id": uid,
>         "ip": request.remote_addr,
>     })
>     ```
> - [ ] 2\. Plain text
>   - _Human-readable, simple._
>   - ```
>     logger.info(f"User {uid} logged in from {ip}")
>     ```
> - [x] 3\. No logging
>   - _Silence — caller decides. (no preview set for this one)_
> - [ ] 4\. _Notes: Add notes on this design._
>
> </details>
>

# Summary of format options

- Single select — one answer; `multiSelect: false`
- Multi select — many answers; `multiSelect: true`
- Preview pane — code/mockup shown on focus; only works with single select
- Option descriptions — subtitle under each label; works on all formats
- "Other" free-text — always injected automatically by the UI
- Max 4 options — the tool hard-caps at 4, so 5 items isn't possible in one question.
- Multi-select order — the result string reflects the order items were clicked, not the order they appear in the list (you clicked Other first, then Four, Two, Three — that's what came back).
- Notes on code preview — when no notes are typed, the result just says selected preview: <preview text> with no notes field at all.
- When "No logging" (no preview) was selected, the result just says "No logging" with no selected preview: field at all — clean, nothing extra. Compare:
  - Option with preview selected → "No logging" selected preview: # nothing emitted here
  - Option without preview selected → "No logging"

# Notes
- Pretty much every question had an "other" kind-of field:
  - If a custom answer is given, use `Type something:`, otherwise use `Type something.`.
    - For a multi-select all entries are added by ` ,`, but may not be in order of the list, but in order of the user selecting it.
    - The custom one is always last, this is confirmed.
      - In **Numbers 1-4**, I typed the note first, then selected the other fields, yet the note is last.
  - If using a `preview` field, the custom answer will be `_Notes:_` vs. `_Notes: Add notes on this design._` instead of `_Type something[.:]_`.
    - You need to handle the `(notes only)` special case, where then the "answer" will be in `annotations[question].notes`.
    - If you select something, instead of the notes, you'll have `annotations[question].preview` instead.
      - Add that to the `<summary>`, btw.
- Reminder that a sub-list element of a numbered list must be indented `len(str(num)) + 2` (`2 = len(". ")`).
  - So for `>> 7. Pick a logging approach (one option has no preview):`  it's 3 spaces more, i.e. `>>    - No logging`.
  - So for `>> 10. Pick a logging approach (one option has no preview):` it's 4 spaces more, i.e. `>>     - No logging`.
