❯ Question answered.
>> **HTML file location** <kbd>Single Select</kbd><br>
>> Where is the Thomann orderlist HTML file (`/mythomann_orderlist.html`)? I couldn't find it in the repo.
> - [ ] 1\. I'll provide it / upload it soon (Recommended)
>   - _You don't have it yet — save a fixture placeholder and implement the parser skeleton; the HTML will come later._
> - [ ] 2\. It's on my local machine (not in repo)
>   - _Tell me the full path and I'll read it directly to understand the structure before writing the parser._
> - [x] 3\. _Type something:_
>   - > It needs to be fetched from the domains (EN, DE, ...)
>
>> **Shein login strategy** <kbd>Single Select</kbd><br>
>> For Shein orders: the existing Shein product provider uses Playwright + curl_cffi to bypass anti-bot challenges. The orders list page at de.shein.com/user/orders/list requires login. Which login strategy should we use?
> - [ ] 1\. Playwright (browser automation)
>   - _Same approach as AliExpress — automate a headless Chrome browser to log in and scrape the orders page. Handles JS-heavy pages and anti-bot naturally._
> - [ ] 2\. Cookie-based (manual login)
>   - _Reuse the existing Shein session infrastructure (cookies/headers from product_dl) — user logs in manually via browser, exports cookies, and list_dl uses those. Simpler but requires manual intervention._
> - [ ] 3\. httpx with curl_cffi
>   - _Try direct HTTP requests with Chrome TLS impersonation, similar to how the Shein product provider fetches pages._
> - [x] 4\. _Type something:_
>   - > Try if _`httpx` with `curl_cffi`_, like the **Shein product provider**, is working, otherwise we have to use _`playwright`_ like **AliExpress**.
>
