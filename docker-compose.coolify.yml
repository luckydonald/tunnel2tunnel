services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      DATABASE_URL: postgresql://${SERVICE_USER_POSTGRES:-t2t}:${SERVICE_PASSWORD_POSTGRES}@db:5432/tunnel2tunnel
      HTTP_PORT: "3000"
      SSH_PORT: "${SSH_PORT:-2222}"
      ADMIN_USERNAME: ${SERVICE_USER_ADMIN:-admin}
      ADMIN_PASSWORD: ${SERVICE_PASSWORD_ADMIN}
      STATIC_DIR: /app/frontend/dist
      RUST_LOG: ${RUST_LOG:-info}
    ports:
      - "3000:3000"
      - "${SSH_PORT:-2222}:${SSH_PORT:-2222}"
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  db:
    image: docker.io/postgres:18-alpine
    environment:
      POSTGRES_USER: ${SERVICE_USER_POSTGRES:-t2t}
      POSTGRES_PASSWORD: ${SERVICE_PASSWORD_POSTGRES}
      POSTGRES_DB: tunnel2tunnel
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${SERVICE_USER_POSTGRES:-t2t} -d tunnel2tunnel"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

volumes:
  pg_data:
